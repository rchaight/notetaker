# Notetaker — build-loop progress log

Loop-engineering state file. Read this + PLAN.md at the start of every pass.

## Loop contract

- **Trigger** — self-paced: passes chain immediately (~60s) inside a milestone; the loop BREAKS at every milestone boundary and waits for user review before entering the next milestone.
- **Termination** — all PLAN.md steps checked AND verify green (build + tests + functional exercise of every component) AND user accepts the final composed version. Breaker: 3 consecutive failed passes → notify + halt.
- **State** — PLAN.md checkboxes = cursor · this file = log/streak · git commits = accumulator. One commit per pass containing the code change AND its checked box (atomic progress).
- **Work per pass** — exactly one PLAN.md step: implement → verify → commit.
- **Verify gate (user directive 2026-07-12)** — `scripts/verify.sh 3 --install` before every commit: ALL package tests ×3 rounds (flakiness), cross-module integration tests (PipelineIntegrationTests), both platform builds, /Applications refresh with an 8s process launch check. QA sweeps are delegated at model tiers: sonnet for coverage audits, opus for adversarial untested-failure review.
- **On failure** — append a log row, streak +1, back off (60s → 270s → 1200s). Only success checks a box and resets the streak. At streak 3: push notification, halt, await user input.

## Current state

- **Phase**: M2 complete — milestone checkpoint
- **Current milestone**: M2 done (next: M3 — inline todos + master To-Do list)
- **Failure streak**: 0
- **Awaiting user checkpoint**: **YES — exercise the editor on Mac + iPhone (see checkpoint criteria), then continue the loop**
- **Branch protection**: proceeding unprotected (private repo, GitHub Free; no user objection at checkpoint — revisit anytime)
- **Environment note**: toolchain = **Xcode 27.0 beta** (`DEVELOPER_DIR=/Applications/Xcode-beta.app`, macOS/iOS 27 SDKs, iOS 27 sim runtime installed) per user directive. Deployment floor 26.0 until a 27-only API is needed. Developer ID cert present (team 6A2NHN89Q8). ⚠️ Repo is inside iCloud-synced ~/Documents — ALL build products must use external paths (see CLAUDE.md).

## Milestone checkpoints

| Milestone | Completed | User reviewed |
|---|---|---|
| M0 — Repo bootstrap (Xcode project, packages, iCloud entitlements, CI) | ✅ 2026-07-10 | ✅ 2026-07-10 |
| M1 — iCloud storage + sync skeleton (VaultKit) | ✅ 2026-07-11 | ✅ 2026-07-11 (two-device sync verified both directions on Mac + iPhone; live conflict drill optional/deferred — machinery unit-tested) |
| M2 — Markdown editor, Liquid Glass (MarkdownKit + EditorKit) | ✅ 2026-07-11 | ✅ 2026-07-11 (checkbox + full marker hiding fixes applied; Writing Tools absent system-wide on this beta — revisit on next macOS 27 seed) |
| M3 — Inline todos + index + master To-Do list (IndexKit + TaskEngine) | ✅ 2026-07-11 | ✅ 2026-07-11 (user: "worked out great") |
| M4 — Todo depth: recurrence, dates, labels, filters | ✅ 2026-07-12 | ✅ 2026-07-12 |
| M5 — Import/conversion (File-Parser/Docling reuse) | ✅ 2026-07-12 | ✅ 2026-07-12 (native tier + docling-serve + import-inbox; deferred: local XPC engine, Share Extension, VisionKit scanner; audio dropped from scope) |
| M5 — Import/conversion pipeline (File-Parser/Docling reuse) | | |
| M6 — AI features (Apple Intelligence / Ollama) | ✅ 2026-07-12 | pending (deferred: PCC deep tier, auto-tag/title, Keychain URL storage, Bonjour, IndexedEntity→M9) |
| M7 — Project management / Gantt (ProjectKit) | | |
| M8 — Security hardening (SecurityKit) | | |
| M9 — Platform surfaces (widgets, Shortcuts, share ext, Watch) | | |
| M10 — Release: signed + notarized .app in .dmg, TestFlight | | |

## Pass log

| # | Date | Step | Result | Notes |
|---|---|---|---|---|
| — | 2026-07-10 | scaffold | ✅ | Loop contract written; awaiting research workflow + PLAN.md |
| — | 2026-07-10 | research + plan | ✅ | 12-agent workflow done: RESEARCH.md (10 sections), FEATURES.md, PLAN.md (68 checkbox steps, M0–M10). Paused at pre-build checkpoint. |
| 1 | 2026-07-10 | M0.1 repo init + .gitignore/LICENSE/README | ✅ | git init -b main, remote → rchaight/notetaker; docs included in initial commit |
| 2 | 2026-07-10 | M0.2 Xcode project (XcodeGen) | ✅ | macOS build green. Ad-hoc signing (no Apple Development cert yet — user must sign into Xcode before M1). iOS sim platform installed; iOS Simulator build green. |
| 3 | 2026-07-10 | M0.3 ten local packages + stub tests | ✅ | 10/10 `swift test` pass; app builds on macOS + iOS Simulator with all packages linked |
| 4 | 2026-07-10 | M0.4 SPM deps: swift-markdown → MarkdownKit, GRDB → IndexKit | ✅ | Deps resolved and exercised by tests (parse blocks; in-memory SQLite query) |
| 5 | 2026-07-10 | M0.5 app shell: adaptive tabs + NavigationSplitView + Settings | ✅ | Builds green macOS + iOS Simulator; Liquid Glass from 26 SDK |
| 6 | 2026-07-10 | M0.6 sandbox + hardened runtime + NSUbiquitousContainers | ✅ | Launch-verified on macOS; entitlements confirmed in signed binary. iCloud container entitlement deferred to M1 (needs real cert). |
| 7 | 2026-07-10 | M0.7 CI (GitHub Actions) + CLAUDE.md + SwiftFormat | ✅ | ci.yml: package tests + both-platform builds + format lint on macos-26 runner; repo formatted clean locally |
| 8 | 2026-07-10 | M0.8 push + first CI + branch protection | ✅ | Pushed to rchaight/notetaker; CI run #1 green (build-and-test + format). Protection 403: private repo on Free plan — user decision. |
| 9 | 2026-07-10 | M0 done-criteria verify | ✅ | App launches on macOS (sandboxed) and iOS Simulator (iPhone 17 Pro). iCloud container provisioning deferred to M1 (needs Xcode sign-in). **M0 COMPLETE — checkpoint.** |
| 10 | 2026-07-10 | ENV: switch to Xcode 27 beta (user directive) | ✅ | Builds + 10/10 package tests green on macOS/iOS 27 SDKs. Diagnosed intermittent codesign "detritus" failures → iCloud-synced ~/Documents decorating in-repo build artifacts; all build output moved to ~/.cache/notetaker-build. |
| 11 | 2026-07-10 | M1 gate: real signing + iCloud entitlements live | ✅ | Apple Development cert auto-minted via -allowProvisioningUpdates; device + App ID + container iCloud.com.rchaight.notetaker registered; CloudDocuments/ubiquity entitlements verified in binary; launch OK. M0 fully done incl. deferred container provisioning. |
| 12 | 2026-07-10 | M1.1 VaultKit: ubiquity container + NSMetadataQuery observer | ✅ | UbiquityContainer.documentsURL, VaultItem + DownloadState mapping, MetadataQueryObserver → AsyncStream snapshots. 9 unit tests (pure parts); live-container runtime proof lands with M1.6 debug harness. |
| 13 | 2026-07-10 | M1.2 coordinated I/O + presenter + debounce | ✅ | VaultFileStore (coordinated read/write/delete/move, startDownloading), VaultPresenter (NSFilePresenter → change stream), Debouncer actor. 17/17 tests. One Swift 6 isolation fix (nonisolated let). |
| 14 | 2026-07-10 | M1.3 folder CRUD + external-mutation tolerance | ✅ | createFolder, folder move-with-contents, VaultEnumerator tree snapshots (skips vanished items/dangling symlinks); uncoordinated external deletion leaves store usable. 23/23 tests. |
| 15 | 2026-07-10 | M1.4 conflict detection + keep-both resolution | ✅ | ConflictNaming (sibling-pattern detect, collision-safe keep-both names), VaultConflictCenter over NSFileVersion, coordinated copy(). 30/30 tests; live conflict exercise in M1.6 two-device matrix. |
| 16 | 2026-07-10 | M1.5 DispatchSource tree watcher + security-scoped bookmarks | ✅ | DirectoryWatcher (per-directory sources, self-refreshing watch set, event-driven test passes); VaultBookmark make/resolve/withAccess with unsandboxed fallback. 33/33 tests. |
| 17 | 2026-07-10 | M1.6 debug harness + REAL-container smoke | ✅ | Settings→Vault live browser (download badges, keep-both button, create/delete). Headless smoke vs real ubiquity container: **VAULT SMOKE OK** (write/read/enumerate/delete round-trip). iOS build green. **M1 automatable steps done — checkpoint: two-device matrix needs user's iPhone.** |
| 18 | 2026-07-11 | FIX: Vault harness invisible on iPhone (user-reported at checkpoint) | ✅ | Vault tab added to the main shell on all platforms (#if DEBUG); removed desktop-sized frame. Verified visually in iOS Simulator screenshot. |
| 19 | 2026-07-11 | M1 CLOSED at checkpoint | ✅ | User verified two-device sync both directions (Mac ↔ iPhone 16 Pro Max, real iCloud). Device install required GUI run (xcodebuild CLI can't see Xcode account session) + verification unblock (VPN/Private Relay + restart). **M1 COMPLETE.** |
| 20 | 2026-07-11 | M2.1 MarkdownKit: document model + styling ranges | ✅ | MarkdownDocument (frontmatter split/round-trip, shallow k:v), MarkdownStyler → UTF-16 NSRanges via UTF-8→UTF-16 SourceConverter (emoji-safe), MarkupWalker covering headings/strong/em/strike/code/links/quotes/tasks/tables. 13/13 tests. |
| 21 | 2026-07-11 | M2.2 EditorKit: TextKit 2 editor + theme + live highlighter | ✅ | MarkdownTheme (kind→attributes, heading scales, mono code), MarkdownHighlighter (attribute-only, never mutates chars), MarkdownEditor NS/UIViewRepresentable (TextKit 2, restyle every keystroke, undo, no smart quotes). 8/8 tests; both app builds green. |
| 22 | 2026-07-11 | M2.6 Notes tab: vault list + editor wired (reordered before 2.3–2.5 for user value) | ✅ | NotesModel (live vault list, coordinated load, 800ms debounced autosave, create/delete, LOCAL-VAULT FALLBACK when iCloud off) + NotesView (split view, ⌘N, context delete, fallback banner). Sim screenshot verified; Mac app reinstalled to /Applications. |
| 23 | 2026-07-11 | M2.3 Live Preview: syntax hiding off cursor line | ✅ | SyntaxMarkers (pure marker math: #/**//~~/`/link plumbing, emoji-safe, 7 tests) + hiddenMarkerAttributes + cursor-paragraph tracking with restyle-on-paragraph-change, both platforms. 30 MarkdownKit+EditorKit tests. Mac app refreshed. |
| 24 | 2026-07-11 | M2.4 editor chrome: mode toggle + glass capsule | ✅ | Source Mode ↔ Live Preview toolbar toggle (⌘/), Liquid Glass word-count capsule (glassEffect .capsule). Both builds green; Mac app refreshed. Full macOS CommandGroup menus deferred to M9. |
| 25 | 2026-07-11 | M2.5 Writing Tools enabled | ✅ | writingToolsBehavior = .complete on NSTextView + UITextView (TextKit 2 gives the rest). User-verifiable: right-click → Writing Tools on Mac. |
| 26 | 2026-07-11 | M2.7 performance: benchmark + keystroke debounce | ✅ | 50k-word styling benchmark (~150ms debug, guarded <3s in CI); restyles debounced 150ms above 20k chars on both platforms (small notes stay instant). One patch-miss fix on the iOS coordinator. |
| — | 2026-07-11 | **M2 COMPLETE — checkpoint** | ✅ | 7/7 plan steps done (passes 20–26). Deferred within-milestone: Focus Mode, NSUserActivity restoration, macOS CommandGroup menus (→ M9), visible-range styling (until real lag observed). |
| 27 | 2026-07-11 | FIX (M2 checkpoint feedback): hide ALL markers in Live Preview | ✅ | Blockquote '>' prefixes (incl. nested, deduped), fenced-code ``` lines, list bullets '- '/'1. ' now hide off-cursor-line; unchecked '[ ]' styled accent (visible as UI, interactive in M3). 37 tests. Awaiting user word on whether Writing Tools menu is missing. |
| 28 | 2026-07-11 | FIX (M2 checkpoint feedback): clickable task checkboxes | ✅ | TaskCheckboxes (token find + toggle, 5 tests) pulled forward from M3; token styled semibold-mono accent; click via .link intercept (macOS, undo-safe insertText) / targeted tap gesture (iOS). 44 tests total. |
| 29 | 2026-07-11 | M2 checkpoint closed: Writing Tools diagnosed as OS-level | ✅ | Missing in TextEdit too → macOS 27 beta seed issue, not app code (FoundationModels reports model AVAILABLE — M6 AI features unaffected). isRichText experiment reverted. Checkbox + marker-hiding fixes user-approved. **M2 reviewed ✓.** |
| 30 | 2026-07-11 | M3.1 IndexKit: GRDB schema + migrator + FTS5 | ✅ | Note/Task/TaskLabel/OutLink records, cascade deletes, noteFTS (BM25 search, replace-on-update), user_version guard → wipe+rescan on mismatch, wipeAllRows. 6 tests incl. mismatch-wipe. |
| 31 | 2026-07-11 | M3.2 inbound pipeline: scan → parse → index | ✅ | NoteScanner (tasks w/ line numbers, wikilinks, tags — 5 tests), TaskTokenParser (>dates incl. weekday-next, !p1–4/high/med/low, #labels — 6 tests, THE one parser for all surfaces), NoteIndexer (SHA-256 skip, transactional upsert, remove, rescan-converges — 6 tests). |
| 32 | 2026-07-11 | M3.3 outbound toggle primitive | ✅ | TaskLineToggler: flip at anchor line, relocate moved lines by exact rawLine match, refuse on content drift, byte-exact elsewhere. 5 tests (42 total in MarkdownKit). |
| 33 | 2026-07-11 | M3.4 TaskEngine smart buckets | ✅ | SmartBuckets: overdue/today/upcoming/inbox by ISO due date, day-boundary safe, garbage dates → inbox (never dropped), isoDay helper. 10 TaskEngine tests. |
| 34 | 2026-07-11 | M3.5 master To-Do tab live end-to-end (+ M3.6 smart views) | ✅ | VaultIndexService (root resolve + fallback, on-disk index, incremental reindex, prune, placeholder-safe, toggle via TaskLineToggler + coordinated IO), TodoView (Overdue/Today/Upcoming/Inbox sections, priority colors, tap-to-complete). TWO BUGS FOUND+FIXED in verify: double-start race on shared DB; metadata-snapshot-driven prune wiped index on local vaults (observers are now triggers only; disk enumeration is authoritative). Sim DB verified: tasks/labels/FTS correct and persistent. |
| 35 | 2026-07-11 | Sync-status badges (user feedback: iPhone sync feels slow) | ✅ | Note rows show live iCloud transit state: uploading ↑, not-downloaded ↓, newer-syncing ⟳, conflict ⚠ (no badge = synced). Latency itself is the iOS daemon's scheduling — surfaced, not hidden. |
| 36 | 2026-07-11 | M3.7 vault-wide FTS search in Notes list | ✅ | .searchable over BM25-ranked noteFTS results; FTS5 operator chars sanitized via quoted prefix terms. **M3 COMPLETE — checkpoint.** |
| 37 | 2026-07-11 | M4.1 ONE recurrence engine, all surfaces | ✅ | Grammar: &every N day/week/month/year/weekday (fixed, catches up overdue series) + &after N (completion-based). RecurrenceEngine.completeTaskLine = THE primitive: recurring lines advance >date and stay [ ] — editor click, To-Do toggle, and service all route through it. Schema v2 (recurrence col, auto wipe+rescan). 21 TaskEngine tests. |
| 38 | 2026-07-11 | FIX: launch crash on macOS (user crash report) | ✅ | Root cause: .searchable's toolbar item crashes SwiftUI's NSToolbar bridge on macOS 27 beta (exception in _insertNewItemWithItemIdentifier) — macOS now uses an in-sidebar search field, iOS keeps .searchable. Also purge saved window state at launch (dev toolbar churn). LESSON: every reinstall must pgrep-verify launch — pass 36's crash slipped through an unverified install. Verified: launch + quit/relaunch OK. |
| 39 | 2026-07-11 | FIX: duplicate New Note button (user-reported) | ✅ | Sidebar returned two sibling views after the search-field fix; group modifiers (.toolbar) applied to each → duplicate. Wrapped in one VStack. Search confirmed working by user. Launch-verified. |
| 40 | 2026-07-11 | M4.2 NL Quick Add (⇧⌘N) | ✅ | QuickAddParser: explicit tokens win, NSDataDetector natural dates (tomorrow/friday/jul 20), bare p1–p4; appends to Inbox.md via coordinated write + immediate index. Sheet on To-Do tab with grammar hint. 27 TaskEngine tests; launch-verified. |
| 41 | 2026-07-12 | M4.3 start (~) vs due (>) dates | ✅ | ~start token (same date vocabulary), Things-style bucketing: unstarted tasks stay out of Today (overdue still wins), 'from <date>' chip. Schema v3. 32 TaskEngine tests; launch-verified. |
| 42 | 2026-07-12 | M4.4 saved filters + label axis | ✅ | TaskFilter grammar (p1–4, due:today/overdue/week/none, #label, note:, free text; AND semantics), evaluated storage-free in TaskEngine; filter bar + saved filters menu (@AppStorage) on To-Do tab; labelsByTaskId lookup. 37 TaskEngine tests; launch-verified. |
| 43 | 2026-07-12 | QUALITY pass (user directive): verify gate + integration tests | ✅ | scripts/verify.sh (tests ×3, builds, install+launch check); PipelineIntegrationTests exercising MarkdownKit+TaskEngine+IndexKit lifecycles (toggle/recur/rescan-converge, editor≡list completion, 5-round idempotence, QuickAdd round-trip). 147 tests ×3 rounds green. Sonnet coverage auditor dispatched; opus adversarial review next. |
| 44 | 2026-07-12 | M4.5 nested subtasks + progress | ✅ | Indent-aware scanning (tab=4), parentId via enclosing-task stack (schema v4), master list = top-level only with n/m checklist badge (green when complete), direct-children progress query. Verify gate ×3 green incl. new nesting tests. |
| 45 | 2026-07-12 | M4.6 Board/Agenda/Matrix views | ✅ | Segmented switcher on To-Do: Kanban columns per bucket, chronological agenda (undated last), Eisenhower 2×2 (urgent=overdue/today, important=p1/p2). All honor the active filter; same toggle primitive. EventKit → M9. Verify ×2 green. |
| 46 | 2026-07-12 | HARDENING: audit findings — 4 real bugs fixed + 20 tests | ✅ | Bugs: (1) CRLF files never split — "\r\n" is ONE Swift grapheme, split(separator:"\n") silently no-ops → shared splitLines() scalar-level splitter across Frontmatter/NoteScanner/TaskLineToggler/SourceConverter; (2) &every-0-days infinite loop → interval≥1 + hang-proof loop; (3) impossible dates (>2026-13-45) stored as metadata → validated, left visible; (4) bullet-hide window 12 vs token window 24 diverged at depth → unified 24 (5-level test). Also: due:week excludes overdue; wikilink regex rejects '['; unicode filenames, empty/1MB files, Debouncer-after-finish, deleted bookmarks, watcher new-subdir, cascade scoping, reopen persistence, wipe→rescan recovery all tested. verify.sh error-reporting fixed. 153 tests ×3 green. |
| 47 | 2026-07-12 | M5.2+5.4 native conversion tier + Import UI (reordered: native-first) | ✅ | ConversionService protocol; NativeConverter: PDFKit text (+Vision OCR for scanned pages), image OCR, RTF/HTML→markdown (headings by font size, bold/italic runs), txt/md passthrough. Import button (Notes toolbar) → Imports/<name>.md with provenance frontmatter → indexed. 9 ConversionKit tests incl. LIVE OCR round-trip + real PDF. Sample PDF planted in ~/Downloads. |
| 48 | 2026-07-12 | FIX (user-reported): Import invisible — verify gate had TWO silent holes | ✅ | (1) xcodebuild\|grep -q dies of SIGPIPE under pipefail → build result never checked; (2) --install then copied a STALE DerivedData app → passes 45–47 shipped yesterday's binary (no Board views, no Import). Also: Result<String,String> compile error + missing xcodegen for new files were hiding behind it. verify.sh now: xcodegen first, exit-code build checks, hard-fails before install. Import UI: grouped toolbar + always-visible bottom-bar button (⇧⌘I). |
| 49 | 2026-07-12 | M5: DoclingServeConverter + router + Settings | ✅ | HTTP tier (multipart /v1alpha/convert/file, tolerant md_content parsing, /health probe, 10-min resource timeout); ConversionRouter: native-first, docling for DOCX/PPTX/XLSX/CSV, descriptive failures; Settings→Document conversion (URL + Test Connection + docker hint). 17 ConversionKit tests (URLProtocol-stubbed server, serialized suite). Works from BOTH platforms. |
| 50 | 2026-07-12 | M5: import-inbox (Imports/Inbox auto-convert) + SCOPE: audio dropped | ✅ | Non-md drops in Imports/Inbox convert on scan and are consumed; failures stay put for a capable device (= iOS→Mac relay). LIVE end-to-end verified: RTF dropped in real vault → running app converted → provenance .md, inbox empty. Audio transcription removed from scope (user's voicetype.app). User already importing real PDFs. |
| 51 | 2026-07-12 | M6.1–6.3 AIKit core + editor AI menu | ✅ | AIProvider protocol + AITask (emits the one grammar), NoneProvider (QuickAdd parse, extractive summary, action-line heuristics), FoundationModelsProvider (@Generable parse/extract, summarize — LIVE generation CLI-verified on this Mac), availability-ordered AIRouter. Editor ✨AI menu: Summarize (block after frontmatter) + Extract Action Items (## section of real - [ ] todos → indexed). FLAKE FIXED: NSTextStorage in parallel test executors aborted intermittently → suites @MainActor+serialized. 9 AIKit tests; gate ×3 green. |
| 52 | 2026-07-12 | M6: OllamaProvider + size-aware routing + Settings | ✅ | Thin /api/chat client with JSON-schema structured extraction (Ollama format param), /api/tags availability + model picker, Settings section (URL/Test/model). Router now skips providers whose contextLimit the input exceeds (FMF ~3k → Ollama for long notes). 14 AIKit tests (stubbed server, serialized). |
| 53 | 2026-07-12 | FIX (user-reported): Ollama Settings section never landed | ✅ | Pass-52 python patch silently no-op'd (formatter had rewrapped the anchor); properties + routing landed, UI section didn't. LESSON: assert patch replacements matched (grep after edit) — 'patched' echo proves nothing. Section applied via anchored Edit; grep-verified; gate green. |
| 54 | 2026-07-12 | M5/M6: onboard File-Parser engine + Ollama localhost fix — and TWO more silent patch no-ops found | ✅ | ARCHITECTURE: macOS drops App Sandbox (Developer ID .dmg; iOS unaffected) → PythonEngineConverter drives the installed File-Parser venv (resolution: setting → app-support → dev repo), LIVE CSV→md verified. Router: native → local engine → docling-serve. ServerURL.normalize fixes bare host:port ("localhost:11434" parsed as scheme). AUDIT OF PAST PATCHES: pass-49 service router patch had ALSO no-op'd — importFile still used NativeConverter (docling-serve was never reachable from imports); fixed via verified Edits. NSLocalNetworkUsageDescription added for iOS→homelab. 20 ConversionKit tests incl. live engine. |
| 55 | 2026-07-12 | M6: semantic search (embeddings) | ✅ | AppleEmbeddingProvider (NLContextualEmbedding, mean-pooled, asset-request), EmbeddingChunker (paragraph windows ≤800ch), noteChunk table (schema v5) + cosine ranking; service embeds changed notes off the hot path; search = FTS first + semantic extras (score>0.45). LIVE meaning-ranking test green. |
| 56 | 2026-07-12 | Settings aligned with File-Parser.app (user request) + restored lost import UI | ✅ | Conversion section: local-engine status + folder override (fileParserEngineDir), OCR + table-structure toggles → CLI --no-ocr/--no-tables. Restored bottom-bar Import button + import status line (lost in an earlier patch cycle). Gate ×2 green. |
| 57 | 2026-07-12 | NOTES EXPANSION (user direction): folder tree + note tabs | ✅ | Sidebar: hierarchical DisclosureGroup tree from real vault dirs (search stays flat/ranked), New Folder (toolbar + alert), Move To context menu (coordinated move, tabs/selection follow). Editor: tab strip for open notes (switch/close, capsule selection). Sonnet researcher delivered 12 ranked organization options for user choice. |
| 58 | 2026-07-12 | NOTES: Inspector panel — outline + backlinks + unlinked mentions (user picked 11/12 options) | ✅ | IndexKit: backlinks(toTitle:) over outLink + unlinkedMentions via FTS-phrase minus linked minus self (tested). Editor gains scrollTarget binding (jump-to-heading). ⌥⌘0 Info inspector: outline (indent by level), backlinks, mentions — tap opens/jumps. Remaining approved queue: tag tree, autocomplete, recents/pinned/bookmarks/saved-searches, daily+templates, multi-vault. |
| 59 | 2026-07-12 | FIX (user crash report): .inspector layout exception on macOS 27 beta | ✅ | NSException in _postWindowNeedsUpdateConstraints during .inspector's toolbar/constraint integration — same beta family as the .searchable crash. Replaced with a plain HStack split panel (identical function, no system inspector machinery). BETA RULE: avoid SwiftUI modifiers that inject into NSToolbar (searchable, inspector) on macOS 27 seeds. |
| 60 | 2026-07-12 | FIX (user-reported ×2): runaway window growth + phantom middle pane | ✅ | Growth: NSTextView now hard-wraps (widthTracksTextView, no horizontal resize), editor pane bounded ideal 720, tab strip maxWidth ∞-leading, WindowGroup .defaultSize 1150×720 (saved-state purge removes frame memory). Phantom pane: NavigationLink(value:) rows in a split-view sidebar infer a 3-column layout — replaced with plain .tag rows; selection drives detail. NOTE: pane fix landed one commit late — the first attempt's anchor missed (assert caught it) but the commit message overclaimed; corrected here. |
| 61 | 2026-07-12 | FIX (user-reported): blank note list at launch until tab-switch | ✅ | Cold ubiquity resolution (seconds) blanked the sidebar, and tab switches recreated NotesModel (losing tabs/selection, re-triggering layout quirks). Now: instant list from cached lastVaultRoot with iCloud confirming in background; NotesModel lifted to AppShell so it survives tab switches. |
| 62 | 2026-07-12 | FIX (user-reported ×3): off-screen width persisted, empty list on cold start, fixed-wide tab sidebar | ✅ | Scene storage restores runaway frames independently of the saved-state purge → launch clamp resizes any window beyond the visible screen. Cold start now uses the DETERMINISTIC container path (~/Library/Mobile Documents/iCloud~…/Documents) — no dependence on a cached prior run (iOS-gated: helper returns nil there). Beta's tab sidebar is fixed-wide/non-resizable → macOS uses compact top tabs; note-list column now min 200 / ideal 260 / max 420 and drag-resizable. |
| 63 | 2026-07-12 | NOTES: in-list creation (user request; top-tab redesign approved) | ✅ | Folder context menu: New Note Here + New Subfolder…; createNote(in:)/createFolder(named:in:) folder-aware with per-folder name uniqueness. |
| 64 | 2026-07-12 | EDITOR: formatting toolbar (user request) + root New Folder row | ✅ | MarkdownEditing (pure transforms: wrap/unwrap bold-italic-strike-code, line-prefix toggles that swap list markers instead of stacking, heading set/clear, link template selecting the url placeholder — 8 tests, 20 total EditorKit). Command binding executes minimal edits via insertText (undo-granular). Format bar over the editor: style menu (Title/Heading/Sub/Body), B/I/S/code, bullet/numbered/todo/quote, link — ⌘B ⌘⇧I ⌘E ⌘⇧T ⌘K. List gains a root 'New Folder…' row. |
| 65 | 2026-07-12 | FIX (user hang report, 44s main-thread loop): one-shot format commands | ✅ | insertText inside updateNSView re-triggers view updates BEFORE the async binding clear → the command re-executed every layout pass (stackshot showed the loop precisely). Commands are now UUID-stamped EditorCommandRequest; the coordinator records the executed token synchronously so re-entrant updates skip. First fix attempt failed pre-write asserts (stale anchors) and never shipped — reapplied against actual file text. |
| 66 | 2026-07-12 | EDITOR: real list glyphs + smart list typing (user request) | ✅ | Return continues bullets/numbers/todos (increments "N. ", "- [x] "→"- [ ] ", empty item ends the list); Tab/Shift-Tab nest/outdent list lines (macOS doCommandBy, iOS shouldChangeTextIn). ListGlyphSubstitution: TextKit2 NSTextContentStorageDelegate paragraph swap renders "- [ ]"→☐, "- [x]"→☑, "- "→• with EQUAL UTF-16 length (figure-space padding) so display offsets == backing store — cursor math, selections, and checkbox-click links all stay valid; file bytes never change. Ordered numbers now always visible (SyntaxMarkers stops hiding list prefixes). @preconcurrency for the MainActor coordinator's delegate conformance. 12 new tests (8 typing + 4 glyph incl. equal-length + link-survival invariants). |
| 67 | 2026-07-12 | M6.5: wikilink + ==highlight== styling | ✅ | New MarkdownElementKind cases wikilink(target:)/highlightMark via post-parse regex scan (swift-markdown can't emit non-CommonMark syntax); matches inside code spans/blocks skipped. [[ ]] and == == hide off-cursor like other markers; wikilinks style accent+underline, highlights get translucent marker-pen tint. 5 new tests incl. code-exclusion and unclosed-syntax guards (53 MarkdownKit total). |
| 68 | 2026-07-12 | M6.5: word count + reading time chip | ✅ | Existing glass word-count capsule now shows "N words · M min read" (~220 wpm, floor 1 min; plain "0 words" when empty). Gate green both platforms. |
| 69 | 2026-07-12 | M6.5: token-based theme palette | ✅ | MarkdownTheme surface tokens: editorBackground (paper white / off-black #1C1C1E-class dark — halation-safe), surfaceBackground (cards), quoteAccent + selectionBackground derived from the accent so caret/selection/links share one hue. Applied to both text views (macOS drawsBackground+insertionPointColor+selectedTextAttributes; iOS backgroundColor+tintColor). Appearance-resolved via NSColor dynamicProvider / UIColor traits. 2 token tests (light≠dark resolution, off-black floor). |
| 70 | 2026-07-12 | M6.5: focus dimming (iA Writer-style) | ✅ | MarkdownHighlighter gains dimOutside: — everything off the cursor's paragraph recedes to tertiary label; marker hiding reordered LAST so its .clear survives the dim. Both coordinators restyle on paragraph change when livePreview OR focus is on; focusMode plumbed through the representables (change detection in update*View). Format-bar toggle (circle.circle), persisted via @AppStorage. 2 tests: dim-region math + hidden-markers-stay-clear ordering. |
| 71 | 2026-07-12 | M6.5: blockquote accent bar | ✅ | First TextKit2 custom drawing: QuoteBarLayoutFragment (NSTextLayoutFragment subclass) paints a rounded 3.5pt accent bar down the quote paragraph's left edge, text drawn by super. Coordinators are NSTextLayoutManagerDelegate (@preconcurrency), returning the custom fragment when BlockquoteDetection matches (≤3 spaces then '>'; 4 spaces = code). Quote text indents 14pt clear of the bar via theme paragraph style. Detection unit-tested incl. the 4-space boundary. |
| 72 | 2026-07-12 | M6.5: horizontal rules as drawn dividers | ✅ | RuleLayoutFragment draws a centered 1pt hairline across the row; ThematicBreakDetection handles ---/***/___/'- - -' incl. the 4-space-code and trailing-text boundaries. Off-cursor the dash characters render CLEAR AT FULL SIZE (hiddenMarkerAttributes' 0.01pt font would collapse the row height, squashing the divider) — new dedicated clear pass in the highlighter; on-cursor the raw dashes reappear over the line for editing. 2 new suites (detection matrix + full-size-clear invariant). |
| 73 | 2026-07-12 | M6.5: inline image thumbnails | ✅ | Standalone ![alt](path) lines: highlighter reserves 150pt paragraph spacing; ImageLayoutFragment draws the NSCache'd image aspect-fit rounded below the line (renderingSurfaceBounds extended; context unflipped locally). LOCAL FILES ONLY by design — no network from the layout pass; remote URLs stay raw text. Styler emits .image(source:) (visitImage). Resolution gotcha: fileURLWithPath(relativeTo:) probes disk for directory-ness and DROPS the base's last component when absent → appendingPathComponent+standardized. imageBase (note's folder) plumbed NotesView→editor. 3 new suites; 42 EditorKit tests. |
| 74 | 2026-07-12 | M6.5: code block cards + language badge | ✅ | CodeCardLayoutFragment: each code paragraph draws its slice of one full-width rounded tinted card (per-corner CGPath; 0.5pt vertical overdraw kills seams); top slice draws the language badge via NSGraphicsContext/UIGraphicsPushContext. CodeCardRegions computes content-between-fences (handles ```/~~~, trailing-newline artifact, unclosed fence; indented blocks card-less). highlight() now @discardableResult returns styled ranges so coordinators derive regions without a second parse. BUG FIX RIDE-ALONG: bullet/checkbox glyph substitution now SKIPS paragraphs inside code regions ("- x" in a fence was rendering as •). Theme: codeBlock per-run background removed (card supplies it; was double-tinting). 5 region tests incl. the substitution-guard containment invariant. |
| 75 | 2026-07-12 | M6.5: full table grid rendering | ✅ | Cursor-outside tables draw as a real grid: TableGrid parses rows/cells/separator (outer-pipe strip, :--: separators) + columnLayout from widest-cell measurement (20pt padding, 44pt floor); TableRowLayoutFragment draws header tint, 0.5pt borders, verticals, and cell text at computed columns while the raw pipe text renders CLEAR underneath (offsets/selection stay on the real string). Cursor entering the table restores raw text (highlighter visible-range check; paragraph-change restyle already invalidates layout). 6 tests: parse matrix, source slicing, monotonic layout, not-a-table guard, clear↔reveal toggle. Deferred: cell inline styling, very-wide-table downscaling, escaped pipes. |
| 76 | 2026-07-12 | FIX (user report): checkbox blue line + unclickable glyph; Insert menu (table/image/rule) | ✅ | Root cause: substitution put ☐ BEFORE the "[ ]" token range, so the .link attribute (and the system's blue link underline) styled the padding spaces after it — glyph moved to the token's first character (offset 2) and linkTextAttributes overridden (macOS pointing-hand only, iOS empty) so theme styling wins. New EditorCommand.insertBlock(_:cursorOffset:) inserts a block paragraph blank-line-separated (empty-line in-place, EOF-safe); format bar gains Insert menu: Table (cursor lands in first header cell), Image… (file picker → NotesModel.attachImage copies into vault Attachments/ with unique name + security-scoped access, inserts percent-encoded relative ![alt](path)), Horizontal Rule. 5 new tests (glyph-on-token invariant + insertBlock matrix). |
| 77 | 2026-07-12 | FIX (user report): to-do toggle now refreshes the open note; bubble checkboxes | ✅ | VaultIndexService.onNoteMutated fires after toggle/quickAdd file writes; AppShell wires it to NotesModel.reloadIfDisplayed (re-reads ONLY when the mutated note is on screen AND the editor has no unsaved typing — never clobbers keystrokes; double-checks dirty/loadedURL after the async read). Checkbox glyphs restyled to the To-Do tab's bubble look: ○ open / ● done, token font +25%, trailing padding now hair spaces so the bubble hugs its text (lead keeps the "- " indent slot). Glyph tests updated. |
| 78 | 2026-07-12 | M6.6: nested tag tree | ✅ | Schema v6: noteTag(noteId,tag) rows written per index pass from NoteScanner.tags (regex already supported #a/b/c); version bump wipes + rescans per the guard. Queries: tagsWithCounts (per-exact-tag), noteIds(withTag:) prefix-matches nested children. Sidebar gains a Tags section: recursive DisclosureGroup tree (AnyView — opaque types can't self-reference, same as folderGroup) with aggregated ancestor counts; tapping filters the list to that tag subtree with a Clear chip; refreshes on tasksVersion. 2 IndexKit tests (nested query matrix, retag replacement). |
| 79 | 2026-07-12 | M6.6: tag + [[wikilink]] autocomplete (macOS) | ✅ | AutocompleteContext (pure, tested): scans back ≤256 chars in the cursor's line for an unclosed "[[" (no ]]/[[ between) or a whitespace-anchored "#" with tag-charset body; completionStrings aligns full queries ("project/al") to the system's partial-word range ("al") and appends "]]" for links, dropping exact matches so the popup doesn't re-offer a finished token. macOS: textDidChange auto-calls complete(nil) while a token is open; the completions delegate serves tags (index) or note titles (model). iOS DEFERRED (UITextView has no native completion popup — needs a custom suggestion bar; logged). 3 test suites (tag matrix, wikilink matrix, alignment). Process note: first patch died on an indent-ambiguous anchor (12-space lines contain the 8-space substring) — newline-anchored patterns now. |
| 80 | 2026-07-12 | M6.6: Pinned / Recents / Bookmarks sidebar sections | ✅ | Pins/bookmarks live in note FRONTMATTER (pinned:/bookmarked: true) so they sync across devices and stay file-derived: Frontmatter.updating(key:value:) edits the raw block minimally (other lines byte-exact; inserts before the closing fence; nil removes) — 3 round-trip tests. Schema v7: pinned/bookmarked columns on note, derived by the indexer; pinnedNoteIds/bookmarkedNoteIds queries (1 test). Service setNoteFlag: read→frontmatter edit→coordinated write→reindex→onNoteMutated (open note refreshes live). Right-click any note row: Pin/Unpin, Bookmark/Remove. Recents: NotesModel tracks last 8 opened (UserDefaults, device-local by design); sidebar shows top 5. Sections render only when non-empty. |
| 81 | 2026-07-12 | M6.6: saved smart searches | ✅ | While searching, a "Save this search" row tops the results (hidden once saved); browse mode gains a Saved Searches sidebar section — tap re-runs the query (FTS + semantic pipeline unchanged), right-click deletes. Persisted as JSON in @AppStorage (device-local UI state; not vault data by design). |
| 82 | 2026-07-12 | M6.6: daily note spine | ✅ | Today toolbar button (⇧⌘D) opens/creates Daily/yyyy-MM-dd.md (POSIX-locale format; header = date + long weekday line); when a daily note is open the format bar grows ◀ ▶ day navigation that walks the spine, creating missing days on the way. Snapshot re-applied after create so selection is immediate (same pattern as createNote). |
| 83 | 2026-07-12 | M6.6: note templates | ✅ | Any .md under Templates/ becomes a template: sidebar toolbar gains a "New from Template" menu (hidden when the folder is empty); creation expands {{title}}/{{date}}/{{time}}/{{datetime}} (TemplateExpansion in MarkdownKit, POSIX-locale, unknown placeholders pass through — tested), names the note after the template with uniquifying counters, selects immediately. Templates are vault files → they sync like everything else. |
| 84 | 2026-07-12 | M6.6: multi-vault switcher (macOS) | ✅ | VaultRegistry: custom folder vaults persisted as (security-scoped where sandboxed) bookmarks with stale re-mint + directory validation; "icloud" stays the built-in default. Switching writes @AppStorage(activeVault) and NotetakerApp re-.id()s AppShell → fresh NotesModel + VaultIndexService root in the new vault. Custom vaults: NotesModel uses DirectoryWatcher snapshots; index service reuses the local-observer path and opens a PER-VAULT index db (index-<id>.sqlite — rebuildable, so vault switches never cross-contaminate). Toolbar Vault menu: vault list with checkmark, Add Folder Vault… (folder picker; can point at an existing Obsidian vault). iOS stays iCloud-only by design. M6.6 COMPLETE. |
| 85 | 2026-07-12 | FIX (user report): jumpy sidebar; layout = Recents → Vault → curated | ✅ | Two causes: (1) selecting a recent note re-sorted Recents mid-click → insert-only recency (existing entries hold position); (2) the same note rendered in several sections ALL carrying .tag(note.id) — duplicate selection tags make List jump between copies → only main Vault rows carry tags now (SelectableTag modifier); section rows select via tap. Sidebar reordered per user: Recents, then the full vault list (root .md + folders) in a labeled Vault section, then Pinned/Bookmarks/Tags/Saved Searches. |
| 86 | 2026-07-12 | Vault location in Settings + bulk import; FIX unclickable vault rows (user reports ×2) | ✅ | Settings › Vault location: shows the active vault path, Use iCloud Vault / Choose Folder… (VaultRegistry; switch re-roots the shell). Sidebar footer gains "Add to Vault…": bulk-copies picked .md/.txt/.markdown files to the root and whole folders as note trees (coordinated copies, per-item uniquify, skip count reported; conversion-based Import Document… unchanged beside it). CLICK FIX: pass 85's unconditional .onTapGesture consumed clicks on main rows (arrow keys bypass gestures — exactly the reported symptom) — tag and tap are now mutually exclusive per row via one modifier. |
| 87 | 2026-07-12 | M7.1: projects as frontmatter notes + Projects tab | ✅ | ProjectKit born: ProjectMetadata.parse (project: true gate; status enum planned/active/on-hold/done with raw fallback; ISO-day validation) + ProjectProgress — 4 tests. Schema v8: note gains isProject/projectStatus/projectStart/projectDue; queries projects()/noteTaskProgress()/tasks(inNote:) — integration test. Projects tab is real: list with status capsule + auto-% progress bar (green at 100%) + due date, detail shows dates and the note's full task list with working check-off (same toggle engine). Notes context menu: Make Project. IndexKit now depends on ProjectKit. |
| 88 | 2026-07-12 | M7.2: dependency grammar + CPM scheduling engine | ✅ | Grammar: ^block-id names a task; blockedby:/depends: (comma lists, optional ^) reference it — parsed/stripped by TaskTokenParser (4 tests incl. math-caret non-match "2^10"). ProjectKit ProjectSchedule: Kahn topological order (nil on cycle; dangling refs ignored — a ghost reference must not wedge the project), CPM forward pass (dependency floor vs explicit dates — explicit start WINS), backward pass → slack + isCritical, dayOffset ISO math (6 tests: cascade, critical chain a→b→c with slack on the branch, cycle, dangling, date-wins, offsets). Schema v9: task.blockId + task.dependsOn columns; indexer writes them (integration test). UI timeline = next step (M7a). |
| 89 | 2026-07-12 | M7a: read-only Gantt timeline (Swift Charts) | ✅ | Project detail gains Tasks/Timeline segmented modes. Timeline: BarMark per top-level task from CPM schedule (day 0 = project start → earliest dated task → today); critical path orange, done green faded, #milestone tasks as purple diamonds; Day/Week/Month zoom via chartXVisibleDomain + horizontal chart scrolling; cycle → explanatory unavailable-view (never a hang); undated+undepended project → "Nothing to Chart" hint with grammar examples. Process: two aborted patch attempts from a wrong count assumption — the pattern-tail disambiguation (inserted copy ≠ closing-brace tail) is why assert messages carry the actual count now. |
| 90 | 2026-07-12 | M7b stage 1: reschedule mutation + drag-move Gantt bars | ✅ | TaskLineRewriter (TaskEngine): settingDueDate/settingStartDate replace/append/remove >due and ~start tokens byte-minimally (CRLF-safe; round-trips through the parser — 4 tests). Service.reschedule mirrors toggle exactly (locate with drift-refuse → rewrite → coordinated write → reindex → onNoteMutated) — SHARED ENGINE for M7.5 swipe-snooze and drag-reschedule later. Timeline: long-press(0.25s)-then-drag moves a bar (plain drag still pans the scrollable chart); live shifted preview during the gesture, one file write on release; day delta from chart-proxy pixel math; ~start only rewritten when the line already had one. Cross-file dep note: hit-test is by category title (first match on duplicate titles — acceptable v1, logged). |
| 91 | 2026-07-12 | M7b stage 2: edge-resize + Blocked By dependency menu | ✅ | TaskLineRewriter grows ensuringBlockId (slug from clean text, 24-char cap, dedup dashes; no-op when ^id exists) + addingDependency (idempotent) — 2 tests, 53 TaskEngine total. Service.addDependency: one read→two line rewrites→one write (target gains ^id if needed, dependent gains blockedby:^id; same-note guard, drift-refuse). Project task rows: link glyph on dependent tasks + right-click "Blocked By ▸" picker. Timeline: press within 14px of a bar's right edge = RESIZE (due-only rewrite, start pinned, floor at start day) vs move elsewhere; cascade falls out of reindex→reschedule recompute. Draw-a-line dependency UX consciously replaced by the menu (logged in plan). |
| 92 | 2026-07-12 | M7: per-project Kanban board — M7 COMPLETE | ✅ | Third project mode: Board. Dependency-aware columns computed from the same rows the timeline schedules: Blocked (waits on an unchecked ^id), Ready, Done; cards show check bubble (live toggle through the one engine), P-badge, due, link glyph. Horizontal column scroller, 250pt columns. M7 done: projects-as-notes, CPM engine, interactive timeline, dependency menu, Kanban. |
| 93 | 2026-07-13 | FIX (user report): Make Project undiscoverable | ✅ | Three visible paths now: (1) Projects tab toolbar + button → name prompt → creates a frontmatter-marked project note with a starter task and selects it (service.createProject: uniquified filename, coordinated write, immediate index); (2) editor toolbar gains a Make Project / Remove from Projects toggle for the OPEN note (isProject lookup); (3) the original right-click item stays. Empty-state text updated to mention the + button first. |
| 94 | 2026-07-13 | FIX (user reports): blank project icon, Make Project no-op, Insert/source into format bar | ✅ | (1) "chart.gantt" is NOT an SF Symbol — every use rendered blank (toolbar button, Projects tab icon, empty states) → calendar.day.timeline.left across 4 files. (2) Make Project silently reverted when the note was open with unsaved edits: the frontmatter write raced the 800ms debounced autosave, which re-wrote the PRE-mutation editor text → new beforeNoteMutation callback awaited at the top of ALL service file mutations (toggle/reschedule/addDependency/setNoteFlag); AppShell wires it to flushSave when the mutated note is selected. (3) Insert menu (table/image/rule, with its fileImporter) and the source/preview toggle moved from the overflow-prone window toolbar onto the editor format bar (toggle right-aligned via Spacer); window toolbar keeps AI/Project/Info + daily nav + Focus. |
| 95 | 2026-07-13 | FIX (user reports): #000 autocomplete leak; title→filename sync | ✅ | (1) Typing "#" with no matching vault tags invoked complete(nil) with zero candidates — AppKit then substituted its own lexicon (hex colors: "#000"). Popup now only fires when OUR filtered candidates are non-empty (hasCandidates check in textDidChange; delegate unchanged — returning [] there is safe once we stop invoking). (2) Obsidian-style title sync: after each successful save, when the first body line is a heading, the file renames to match it (sanitized of /\\:?%*|"<>, dot/space-trimmed, 80-char cap, sibling-uniquified); selectedID/openTabs/recents/loadedURL all remapped, snapshot re-applied; rename failure is silent (save already landed). Repeated renames while typing the title are accepted (Obsidian behaves the same). |
| 96 | 2026-07-13 | FIX (user report): # heading vs #tag ambiguity | ✅ | A bare "#" no longer opens the tag popup — tag context now requires ≥1 tag character after the # ("#w" triggers, "# " never; "##" already excluded). Heading typing is completely popup-free; tags pay one keystroke of popup delay. Grammar note: "#word" (no space) is a tag and NOT a CommonMark heading, so the two syntaxes are cleanly disjoint at the parser level too. Tests updated (+2 heading-marker cases). |
| 97 | 2026-07-13 | NOTES: Favorites section (user request) | ✅ | Third frontmatter flag (favorite: true) through the established pipeline: schema v10 column + favoriteNoteIds() query, indexer derives from frontmatter, service wrapper, star/star.slash context-menu toggle on every note row. Sidebar order now Favorites → Recents → Vault → Pinned/Bookmarks/Tags/Saved Searches. Flag syncs across devices like pinned/bookmarked (files are truth). |
| 98 | 2026-07-13 | FIX (user report): shadowed context menu; folder favorites | ✅ | ROOT CAUSE of both this report AND pass 93's "no Make Project option": noteRow carried TWO .contextMenu modifiers (flags menu added pass 80/87/97 + Move To/Delete menu from an earlier pass) — the outer one shadows the inner completely, so only Move To/Delete ever showed. Merged into ONE menu: Favorites/Pin/Bookmark/Make Project, divider, Move To/Delete. Folder favorites: star toggle on folder rows (device-local @AppStorage — folders have no frontmatter to carry a synced flag); Favorites section renders favorite folders as expandable groups above favorite notes, pruning folders that no longer exist. |
| 99 | 2026-07-13 | FIX (user report): favorite folder expanded empty | ✅ | The favorites copy of a folder rendered the same noteRows (same .tag ids) as the Vault tree below — duplicate List identities collapse and the duplicate group shows nothing. folderGroup gains selectable: (cascades to noteRow and recursive subfolders); Favorites renders tap-to-open copies, Vault tree keeps real selection rows. Same class of bug as pass 85's jumpy selection — noted as a standing rule: any note row rendered OUTSIDE the Vault section must be selectable: false. |
| 100 | 2026-07-13 | FIX (user report): can't favorite notes | ✅ | Two hardenings: (1) the merged context menu re-attached BEFORE SelectableTag (pass 98 had moved it after — beta List rows are modifier-order sensitive). (2) Real logic bug: favoriting the OPEN note right after editing its title line silently no-oped — beforeNoteMutation's flush triggered the title-sync RENAME, moving the file out from under setNoteFlag's URL (read fails → silent return). flushSave gains allowRename:; mutations flush without renaming (title sync catches up on the next debounced save). |
| 101 | 2026-07-13 | FIX (user report): selection highlighted every copy of a note/folder | ✅ | Root cause: selectable Lists auto-tag EVERY ForEach row from its identity — removing the explicit .tag (pass 85) never removed the implicit one, so Favorites/Recents copies of the selected note still highlighted. .selectionDisabled(true) on all non-selectable note rows AND folder labels (folders also carried implicit tags: duplicates highlighted and clicking one set selection to a folder path → performSelect lookup failed → editor cleared). Only the Vault-section copy of a note highlights now; section copies open on tap without touching selection. |
| 102 | 2026-07-13 | FIX (user reports): favorites vanish until next change; clicked-copy highlighting | ✅ | (1) reindexFromDisk only bumped tasksVersion when a file changed — on a clean launch the sections queried the db BEFORE it opened and never refreshed (favorites "reappeared once a new file is added"). The counter now always bumps at scan end. (2) Selection highlight follows the CLICKED copy: List selection decoupled from model.selectedID (listSelection drives Vault rows; onChange syncs programmatic selects); section rows carry a "section|noteId" highlight key rendered via listRowBackground accent tint; clicking a section row clears the Vault highlight and vice versa; favorite-folder groups cascade their section key to nested rows. |
| 103 | 2026-07-13 | FIX (user reports): folder click-to-expand; erratic editor jumps | ✅ | (1) Folder labels expand/collapse on click: programmatic DisclosureGroup expansion keyed "section|folder" (same folder can be open in Favorites, closed in Vault); full-width contentShape. (2) Editor jump root cause: EVERY cursor-paragraph change ran a whole-document restyle, and marker reveal (0.01pt→full size) reflowed text under the click. New revealRanges (markers + table/HR bodies, computed once per restyle): a cursor transition touching none of them now skips the restyle entirely — clicking around plain text is zero-reflow; focus mode still always restyles (dim follows cursor). Both platforms. |
| 104 | 2026-07-14 | M7.5: completion fade (Things-style calm UI) | ✅ | Checking a task in List view fills the bubble, strikes the text, and fades the row to 45% for 400ms BEFORE the toggle writes — the reindex then removes it with the standard list animation. completingIds guards double-taps; button disabled mid-fade. Board/Agenda/Matrix keep instant toggle (their cards re-bucket rather than vanish). |
| 105 | 2026-07-14 | SETTINGS: editor font + size (user request) | ✅ | MarkdownTheme gains fontDesign ("system"/"serif"/"rounded"/"mono") + designedFont(size:bold:) via font-descriptor SystemDesign — body, headings (proportional scales), and bold runs all honor it; code spans/blocks stay monospaced by design. Settings › Editor: font picker + 11–28pt stepper with reset. NotesView builds the theme from @AppStorage; coordinator theme is now VAR and update*View detects size/design changes → restyle (previously theme was frozen at coordinator creation — settings would never have applied live). |
| 106 | 2026-07-14 | M7.5: row density setting | ✅ | Toolbar density menu (Compact/Comfortable/Relaxed, @AppStorage): padding 0/4/9pt; Compact also drops the meta line (due/priority/labels move out of sight) and tightens to .callout — Linear-style scanning mode. Meta line extracted to its own builder. |
| 107 | 2026-07-14 | FIX (user report): frontmatter visible in the editor | ✅ | Frontmatter ("favorite: true" + fences) was rendering as markdown — the closing --- even drew as a divider rule. Live preview now collapses the whole block off-cursor (hair-width, like syntax markers) and reveals it when the cursor enters (still editable on purpose); layout-fragment delegate returns plain fragments inside frontmatter so its fences never draw as rules; block included in revealRanges so the show/hide transition restyles. Test: collapse + reveal round trip. Source mode (⌘/) still shows everything raw. |
| 108 | 2026-07-14 | M7.5: jump-to-note from task rows | ✅ | Double-click a task's text (or right-click → Open in Note) → AppShell switches to the Notes tab (TabView selection now value-based), NotesModel.openNote records the source line, and once the text loads NotesView converts line→UTF-16 offset into the existing scrollTarget (cursor lands on the task's line). List view only for now (Board/Agenda/Matrix cards keep their tap semantics; deferred). |
| 109 | 2026-07-14 | M7.5: priority/label color chips everywhere | ✅ | TaskChips: ONE chip vocabulary — PriorityChip (Todoist P1 red/P2 orange/P3 blue/P4 muted capsules) + LabelChips (#label capsules, deterministic palette color via unicode-scalar fold — NOT hashValue, which is process-seeded — with per-label override via right-click palette picker persisted as JSON). List rows show priority+labels; Board/Agenda/Matrix cards adopt PriorityChip (labels on cards deferred — width). refresh() now always loads the label map (was filter-only). |
| 110 | 2026-07-14 | M7.5: delete-line write mutation | ✅ | TaskLineToggler.removingLine (bounds-safe, CRLF-preserving — 2 tests) + service.deleteTask with the full outbound-write discipline (flush-before-mutation, drift-refuse locate, coordinated write, reindex, onNoteMutated). Set-due-date half of this checkbox shipped in pass 90 (reschedule). Swipe + drag consume these next. |
| 111 | 2026-07-14 | M7.5: swipe actions + row quick actions | ✅ | List rows: full-swipe leading = complete (with the fade), trailing = delete (destructive) + snooze-to-tomorrow (orange, reschedule engine). Works on iOS touch AND macOS trackpad swipes. Context menu gains the same snooze/delete so mouse users aren't left out. tomorrowISO helper (calendar-timezone-aware). |
| 112 | 2026-07-14 | M7.5: drag-to-reschedule (Agenda + Board) | ✅ | Cards/rows are .draggable(task.id); Agenda date sections accept drops (that ISO day; "No date" clears the due token), Board columns map Today→today, Upcoming→tomorrow, Inbox→clears, Overdue rejects (not a meaningful target). All writes through the pass-90 reschedule engine — the markdown line's >date token is what changes. |
| 113 | 2026-07-14 | M7.5: multi-select + batch edit | ✅ | ⌘-click/drag selection in List view (Set-based List selection, rows tagged); a batch bar rises when >1 selected: Complete, Priority P1–P4/Clear, Due Today/Tomorrow/Clear, Delete, Done. New grammar rewrites: settingPriority (replace !high→!p2 style aliases, add, remove) + addingLabel (idempotent) — 2 tests; service.rewriteTaskLine generalizes the outbound-write discipline (setPriority/addLabel wrap it). Batch ops run SEQUENTIALLY (same-note line shifts are safe: locate falls back to exact-raw-line search; identical duplicate lines resolve to first occurrence — acceptable, logged). |
| 114 | 2026-07-14 | M7.5: AI-powered Quick Add | ✅ | quickAdd routes natural-language input through AIRouter.parseTask (new router method) → on-device FoundationModels guided generation; deterministic QuickAddParser remains the fallback AND the fast path (inputs with explicit >, !p, &every tokens skip the model — exact and free). AITask.markdownLine keeps output in the one grammar; empty/failed generations fall through. Ollama intentionally NOT in the quick-add chain (a task capture shouldn't wait on the homelab network). |
| 115 | 2026-07-14 | M7.5: Logbook + completion-date grammar token | ✅ | Checking a task now writes " ✅YYYY-MM-DD" into the source line (unchecking strips it) — completion history lives in the FILE, honoring the one invariant (toggler round-trip test). Parser extracts completedDay (clean-text test); schema v11 task.completedDay indexed column + completedTasks(limit 500) newest-day-first. New "Log" view mode: sections per day, green check reopens (removes the token). Recurring completions keep their advance-the-date semantics (no ✅ — box stays open by design). |
| 116 | 2026-07-14 | M7.5: streak strip (opt-in, local-only) | ✅ | Streaks.compute (TaskEngine, pure): done-today count + consecutive-day streak from ✅days; an unfinished TODAY doesn't zero an ongoing streak (counts from yesterday) — 2 tests incl. gap-kill. Toolbar capsule "N done today · M-day streak", opt-in via density menu toggle (default OFF per the calm-UI research). No server, no karma economy. |
| 117 | 2026-07-14 | M7.5: ⌘K command palette | ✅ | Global sheet (hidden-button hotkey in AppShell): fuzzy note jump (title contains, capped 12), "add <task>" → quick-add pipeline (AI-backed since pass 114), Go to Notes/To-Do/Projects, Today's daily note; Return runs the top hit. Editor's link shortcut yields ⌘K to the palette → ⇧⌘K (help text updated). |
| 118 | 2026-07-14 | M7.5: menu-bar quick capture — M7.5 COMPLETE | ✅ | MenuBarExtra (window style, pencil icon): type a task from any app → QuickAddParser grammar → coordinated append to Inbox.md; vault resolution mirrors the app (custom folder vault → iCloud well-known path); the running app's observers reindex automatically. Registered global hotkey deferred to M9 (Carbon/event-tap territory, belongs with the other system surfaces). ALL 13 M7.5 items shipped. |
| 119 | 2026-07-14 | M8.1: app lock | ✅ | SecurityKit born: AppLockPolicy (pure grace-window logic — 4 tests incl. boundary ±1s and fresh-launch-always-locks) + BiometricUnlock (.deviceOwnerAuthentication = Touch ID/Face ID/Watch/password in one policy). AppShell: material lock overlay auto-prompts on appear; scenePhase tracks backgrounding; launch locks when enabled. Settings › Security: enable toggle + grace picker (Immediately/1m/5m/1h). Content is COVERED not blanked (material blur) — hides without destroying editor state. |
| 120 | 2026-07-14 | M8.2a: locked-note crypto core | ✅ | NoteCrypto: PBKDF2-HMAC-SHA256 (600k rounds, CommonCrypto) → AES-GCM (CryptoKit); LockedNoteFile: plain frontmatter (locked/salt/rounds) + base64 ciphertext body — still a valid syncing .md that never carries plaintext. 4 tests: round trip, wrong-passphrase fails closed, tamper fails closed, file-format round trip asserting plaintext absence. UI/session integration next. |
| 121 | 2026-07-14 | M8.2b: locked notes end-to-end | ✅ | Toolbar Lock Note… → passphrase+confirm sheet with the UNRECOVERABLE warning (min 6 chars); file rewrites as frontmatter(locked/salt/rounds)+base64 AES-GCM body — verify in Finder: no plaintext. Opening a locked note shows a lock placeholder + Unlock sheet; wrong passphrase fails closed. Unlocked notes keep keys IN MEMORY ONLY (quit relocks); every autosave re-encrypts (plaintext never touches disk); Remove Lock decrypts back to plain markdown. Indexer: locked notes index TITLE ONLY — ciphertext never reaches task scanner/FTS/tags. Deferred: Keychain biometric key-wrap (Touch-ID reopen), shared-passphrase session across notes. |
| 122 | 2026-07-14 | M8.3: Ollama endpoint in the Keychain | ✅ | KeychainStore (SecurityKit): generic-password save/read with kSecAttrAccessibleAfterFirstUnlockThisDeviceOnly (homelab topology never rides iCloud Keychain), empty-string = delete; migrateFromDefaults moves the legacy UserDefaults value once and scrubs it. Settings field and the AI-router construction both read/write through it. |
| 123 | 2026-07-14 | M8.4/5: disclosure + runtime audit — M8 COMPLETE | ✅ | Settings › Security: ADP guidance (no public status API exists — nudge with the System Settings path; locked notes E2E regardless). AI section states the privacy policy; per-output provider stamping already shipped with M6 (every AI block carries *(provider)*). Hardened Runtime verified on the installed build: codesign flags 0x10000(runtime), no exception entitlements; sandbox intentionally absent (M5 decision, Developer ID + Process spawning) — re-audit queued for M10 notarization. WKWebView sanitization N/A: no web-preview surface exists (HTML imports convert to markdown). |
| 124 | 2026-07-14 | FIX (user report): GitHub lint failures | ✅ | swiftformat . reformatted 28 drifted files (wrap/indent/trailing-space + noForceUnwrapInTests in EditorKit tests); lint now 0/121. ROOT CAUSE: CI runs swiftformat --lint but scripts/verify.sh never did — drift accumulated invisibly across passes. The gate now lints FIRST (hard-fail with the fix command) so every future commit is CI-clean by construction. Full gate re-run green after the reformat. |
| 125 | 2026-07-15 | M9.1: App Intents (Add Task / Create Note) | ✅ | HeadlessVaultWriter: vault writes without the running UI (root = custom vault → iCloud well-known path; coordinated writes; the ONE task grammar) — menu-bar capture refactored onto it. AddTaskIntent (+dialog prompts, parameter summary) & CreateNoteIntent in the App target (metadata extractor requires it); NotetakerShortcuts registers Siri phrases. GOTCHA: Siri phrases may only interpolate AppEntity/AppEnum — a String parameter in a phrase halts the metadata processor with a build error (text is collected via requestValueDialog instead). Shortcuts app now shows both actions; "Add a task in Notetaker" works via Siri. AppEntity/Spotlight indexing deferred to the deep-link pass. |
| 126 | 2026-07-15 | M9.2: global quick-capture hotkey (macOS) | ✅ | Carbon RegisterEventHotKey ⌃⌥⌘N (no accessibility-permission prompt, works with ad-hoc signing) → floating NSPanel (nonactivating utility window, .floating level) hosting the same MenuBarQuickAddView → HeadlessVaultWriter → Inbox.md. Toggle semantics (press again closes). Menu-bar popover advertises the shortcut. Registered once at app init. |
| 127 | 2026-07-15 | M9.3: link graph + Handoff | ✅ | GraphView: ring layout with degree-weighted radius (hubs drift centerward, node size scales with links, capped 6), Canvas edges from the outLink table (title→node resolution case-insensitive), tap opens the note; toolbar Graph button presents as a sheet. Handoff: open note publishes NSUserActivity (com.rchaight.notetaker.note) and the receiving device continues into the same note. Process note: pass-124's swiftformat had stripped a redundant `public` my anchor assumed — anchors must match current bytes, not remembered ones. |
| 128 | 2026-07-15 | M9.4: Today's Tasks widget | ✅ | New NotetakerWidgets extension target (xcodegen app-extension, com.apple.widgetkit-extension, embedded+signed with the app). Data path: VaultIndexService.publishWidgetSnapshot on every reindex → today-tasks.json in the app-group container (TEAM-PREFIXED group id — "group.*" style demands provisioning on macOS; 6A2NHN89Q8.* signs with the dev cert) → TimelineProvider decode + WidgetCenter.reloadTimelines. Widget: today list (priority-tinted bubbles, 4 rows), overdue badge, "All clear ✨" empty state; small+medium. SIGNING LESSONS: widget entitlements refuse ad-hoc; the APP has been auto-signing with the real dev cert since M1 — forcing "-" anywhere breaks embed parity; both targets Automatic now. Deferred: interactive check-off (intent-in-widget-process), Quick Note widget, Control Center ControlWidget, Watch. |
| 129 | 2026-07-15 | FIX (user report): CI lint failures round 2 — version skew | ✅ | Two violations from rules newer than the local swiftformat (redundantSendable on a private struct, preferCountWhere) fixed manually. ROOT CAUSE: ci.yml installed brew-latest swiftformat while local runs 0.62.1 — the rulesets drift, so local lint can pass while CI fails. CI now downloads the PINNED 0.62.1 release binary (same as local); version printed in the log. Upgrading is now an explicit two-place bump (brew local + ci.yml pin). |
| 130 | 2026-07-15 | UI overhaul (user requests ×3): custom shell + ribbon, To-Do/Vault sidebars, Notes action bar | ✅ | (1) macOS shell rebuilt: beta TabView (blinking tab highlight on keystrokes, bouncy slow transitions) replaced by a custom top tab bar over ALWAYS-MOUNTED views (opacity switching — instant, zero animation, no state loss) + an Obsidian-style 40pt ribbon: new note, daily note, new task (signals TodoView's Quick Add), new project (signals ProjectsView's prompt), ⌘K. iOS keeps adaptive TabView. (2) To-Do gains a sidebar (view modes with icons + saved filters, accent-tinted selection) mirroring Notes/Projects; Vault debug wrapped in a split view. (3) Notes sidebar: compact icon strip above Favorites — new note, import document, add-to-vault (both moved out of footer/toolbar), sort menu (name / recently-modified via VaultItem.modificationDate), collapse-all-folders. |
| 131 | 2026-07-15 | M9.5.1: overwrite-safe note creation (user request); M10 pushed back | ✅ | THE HOLE: uniquifiers checked the in-memory list or local disk — an iCloud note not yet downloaded exists only as ".Name.md.icloud", invisible to both → "Untitled.md" could clobber a remote note across every device. VaultNaming (VaultKit, 4 tests incl. placeholder detection): taken = on-disk OR placeholder OR caller-reserved; uniqueFileName counts past collisions. All creators converted: createNote/in-folder, template, daily (now START-DOWNLOADING an undownloaded daily instead of writing a fresh template over it — worst case fixed), renameToMatchTitle, createProject, HeadlessVaultWriter, attachments. New M9.5 milestone (shakedown + Settings buildout) inserted before M10. |
| 132 | 2026-07-15 | M9.5.2: tag rethink (user report: unwieldy sidebar dump) | ✅ | Three cuts: (1) NoteScanner.noteTags skips TASK lines — "#admin !p1" on todos is a task label with its own To-Do surface, not a note topic (test pins the split; schema v12 rescan applies it to existing rows). (2) Sidebar Tags is now a COLLAPSED disclosure showing only the top 10 by note count (tap = filter toggle). (3) "All Tags (N)…" opens a searchable browser sheet sorted by usage. The old always-expanded nested tree is gone from the sidebar. |
| 133 | 2026-07-15 | M9.5.3: Tag Manager + AI merge suggestions + tooltips (user requests) | ✅ | TagCuration (AIKit, 3 tests): heuristic merges (case/plural/separator variants fold into the higher-count winner) + validated() that drops hallucinated/self merges — MODEL OUTPUT IS NEVER TRUSTED to name real tags. AIProvider.suggestTagMerges (default = heuristics); OllamaProvider override: JSON-schema chat, union of heuristics + validated AI extras. Service: renameTag (boundary regex — nested children untouched; escapedTemplate for $ safety), deleteTag, suggestTagMerges (Ollama when configured → heuristic fallback). All Tags sheet is now a Tag Manager: right-click rename (alert)/merge-into (picker)/remove-everywhere; "Suggest Merges" wand shows reasoned suggestions with Apply/Dismiss. Autocomplete: substringMatch for tags — typing a fresh tag surfaces containing tags ("notes" → "meeting-notes"), cutting duplicate births (test). Tooltips: top tab bar, tab-close ×, every check bubble (todo/project/board). |
| 134 | 2026-07-15 | FIX (user report): unresizable oversized window; Tags gets its own tab | ✅ | WINDOW BUG: the always-mounted ZStack kept every tab's NavigationSplitView TOOLBAR registered with the window — opacity hides pixels, not toolbar registration — and the union of all toolbar rows drove the window minimum past the screen. Tabs render via plain switch now (one view in the hierarchy); cross-switch state stays in the shared models, To-Do's view mode moved to AppStorage so it survives recreation. TAGS TAB: the sheet manager grew into a full page — sidebar (search, wand suggestions with Apply/Dismiss, rename/merge/delete context menus, counts), detail = every note carrying the selected tag with open-in-Notes; Notes sidebar keeps the collapsed top-10 quick filters and points to the tab. Dead sheet code removed. |
| 135 | 2026-07-15 | UI (user request): To-Do sidebar top mirrors Notes | ✅ | To-Do sidebar now leads with the filter field (mirrors Notes search position) + the same compact icon strip: new to-do sits where new note sits (square.and.pencil), save-current-filter in the new-folder slot, Logbook shortcut, density/streaks menu at the right edge (matching the sort-menu slot). Filter bar moved OUT of the detail pane (was duplicated context); density menu left the toolbar. |
| 136 | 2026-07-15 | UI (user request): consistent window toolbars on every tab | ✅ | Every tab's top toolbar now mirrors Notes' creation-first layout: To-Do = New Task (square.and.pencil, same slot/icon as New Note) + Agenda + Logbook + streak chip; Projects' New Project adopts the same icon/slot; Tags = Suggest Merges wand; Vault = Rescan (new public service.rescan()) + Reveal in Finder (service.vaultRootURL). All with hover help. |
| 137 | 2026-07-15 | M9.5: CloudKit task extras + Todoist-style detail window (user request) | ✅ | ARCHITECTURE: markdown line stays the canonical task; TaskExtrasStore (private CloudKit db, container iCloud.com.rchaight.notetaker + CloudKit entitlement added) holds what one line can't (description, link) keyed by the task's durable ^id — service.ensureStableId assigns "t-xxxx" LAZILY (only tasks with extended data gain an id; UUID entropy, survives text edits). Losing CK loses only extras — invariant intact, and the detail view says so. Detail UI: click a task → dedicated WINDOW on macOS (WindowGroup(for: String) — services lifted from AppShell to NotetakerApp so scenes share instances) / sheet on iOS: check bubble, editable title (line rewrite), due DatePicker + Clear (reschedule engine), P0–P4 segmented (setPriority), recurrence display, jump-to-note link, description TextEditor + URL with graceful offline states (notAuthenticated/network messages; cache-first reopen). Context menu gains Open Details; single click = details, note jump stays in menu + detail. CK schema auto-creates in the dev environment on first save. |
| 138 | 2026-07-14 | M9.5: explicit multi-select mode for mass edit/delete (user request) | ✅ | The pass-113 batch engine existed but hid behind ⌘-click — and single-click now opens details. New checklist toggle in the sidebar action bar enters SELECT MODE: rows grow accent selection bubbles, any click toggles membership (detail-open suspended), selected rows tint, and the batch bar appears from the first pick with a new Select All; Done exits and clears. All existing batch ops (Complete / Priority P1–P4+Clear / Due Today-Tomorrow-Clear / Delete) work over the selection. ⌘-click path still works outside select mode. |
| 139 | 2026-07-14 | FIX (user report): detail-window title edits now propagate | ✅ | Two defects: (1) commit only fired on Return — now also on focus loss and window close; (2) the rewrite did string-replace of CLEAN text inside the RAW line, silently no-oping whenever tokens sat mid-text ("- [ ] call >today dean") → TaskLineRewriter.replacingText rebuilds the line: checkbox prefix + new title + every token re-rendered (due/start/!p/&recurrence/^id/blockedby/✅; weekday dates canonicalize to ISO; indent+CRLF preserved — 3 tests). Propagation rides the existing loop: rewrite bumps tasksVersion (main list re-queries) and onNoteMutated reloads the open note editor. |
| 140 | 2026-07-14 | App icon + Projects task flow (user requests) | ✅ | ICON: drawn programmatically (CoreGraphics script, no external tools): blue gradient squircle, white note page with folded corner + markdown bars + blue bullets, green check-circle badge with white ring; 1024 master downsampled via sips to all mac sizes; AppIcon.appiconset (universal-iOS 1024 + mac 16→1024) wired via ASSETCATALOG_COMPILER_APPICON_NAME. PROJECTS: detail's Tasks section leads with an add-task field (⏎ appends through the quick-add grammar — AI-backed — to the project note; service.addTask); clicking any project task opens THE SAME TaskDetailView as To-Do (shared window scene on macOS / sheet on iOS; shared TaskExtrasStore lifted through AppShell). Tasks were already the same objects as the To-Do list (same markdown lines/index) — now the surfaces match too. |
| 141 | 2026-07-14 | To-Do → Project assignment (user request) | ✅ | service.moveTask relocates the RAW line between notes (destination-write-first so failure never loses the task; tokens/^id move verbatim → CloudKit extras follow for free; drift-refuse on source). Three surfaces: task context menu "Move to Project ▸" (+ back to Inbox), detail window gains a Project row showing the current project with a move menu (dismisses after — the line-keyed window id dies with the move), Quick Add gains an "Add to" destination picker (Inbox default / any project → addTask path). |
| 142 | 2026-07-15 | Detail window rebuild + project rollups (user request, screenshot) | ✅ | LAYOUT FIX: scrollable form + pinned Save footer + real min/ideal frame (fields were overflow-hidden). Task detail grows: Status (derived dot — Done green / Blocked orange via unfinished ^deps / Open blue, milestone flag), Start date picker (~token via reschedule), Assignee (@name grammar), Milestone switch (#milestone label add/remove), Blocked-by row (current deps + add menu over the note's open tasks), Objective (new CloudKit extras field). PROJECT rollups in detail header: health dot (red when overdue>0; status/all-done aware) + overdue count, editable Objective (frontmatter via new generic setFrontmatterValue/frontmatterValue), Assignees aggregate (distinct @names), Milestones list (diamond fill = done, with dates), dependency count. Timeline already renders milestone diamonds + critical path. |
| 143 | 2026-07-16 | Tags: multi-select, LLM grouping, descriptions, unified page (user request) | ✅ | ENGINE (committed separately): TagGroup suggestions — prefix heuristics ("meeting-notes"+"meeting-agenda" → meeting/…) + Ollama semantic families (schema-constrained, validatedGroups filters hallucinated/nested/single-member output — 3 tests); IndexKit tasks(withLabel:). UI: Tags sidebar is multi-select (⌘/⇧-click) with a bulk bar (Merge Into…/Delete All); wand now runs merges AND groups concurrently (grouping applies as nested renames); per-tag DESCRIPTION lives in private CloudKit (TagExtras records, cache-first, renames MOVE the record — wired into every rename path); the tag page unifies everything the tag touches: About (description), Projects, To-Dos (label matches, toggle + detail window), Notes (folder context, project notes deduped out). |
| 144 | 2026-07-16 | Drag-and-drop note moves + topic Areas (user requests) | ✅ | Notes drag onto any folder label (coordinated move; multi-item), onto the Vault header (→ root), or onto an AREA row (assigns). Areas = frontmatter area: (syncs, file-derived; schema v14 note.area + areaAssignments query): sidebar Areas section between Recents and Vault (disclosure per area, counts, tap-to-open rows); note context menu Move to Area ▸ (existing/New Area… alert/Remove). High-level organization fully independent of folder placement. |
| 145 | 2026-07-16 | Subtasks + nested-bullet notes in task details (user request, example-driven) | ✅ | Detail window grows a Subtasks section: nested "- [ ]" children (already parentId-linked in the index; the To-Do list keeps showing only parents with the n/m badge) render as a toggleable checklist with done-count, plus an add-subtask field (service.addSubtask inserts "  - [ ] …" at the END of the parent's nested block, indent-derived from the parent's raw line). Nested PLAIN bullets ("- " without checkbox) under the task line surface as a read-only "From the note" list beside the description (service.nestedBullets — blank-line tolerant, stops at the first sibling-or-shallower line), matching the user's dictation convention; CloudKit description stays separate/editable. |
| 146 | 2026-07-16 | FIX (user reports): wand silence + timeouts; tag-popup backspace trap; tag chips | ✅ | (1) Wand: Ollama availability now 5s-capped and suggestion calls 30s-capped (withTimeout task-race helper) — no more endless spinner on a slow homelab; empty results show honest feedback ("tags look tidy" / "add more tags") instead of silence. (2) Backspace trap: the completion popup re-triggered on EVERY text change including deletions, so "#Header" couldn't be backspaced into a heading — auto-trigger now fires only when text GREW. (3) Tags stand out in notes: new .tag(name) styled kind (regex tier, code-excluded) rendered as accent chips (tinted background, semibold, slightly compact) in the editor. |
| 146b | 2026-07-16 | Process note | ✅ | Commit 20aa869 shipped red (the gate failure didn't stop the chained commit — `;` instead of gate-conditional). Repaired one minute later by 8a31357; commits now conditional on gate exit code in the loop discipline. |
| 147 | 2026-07-16 | FIX (user report): task-line tags invisible on the Tags page | ✅ | Pass-132's task-label exclusion was right for the NOTES sidebar (topic decluttering) but wrong for the Tags PAGE — the user's tags (#Amber1 on "- [ ]" lines) vanished from management entirely. Tags page now manages the UNION: allTagCounts merges noteTag counts + taskLabel counts (new taskLabelCounts query); the unified page already showed label To-Dos. CRITICAL companion fix: renameTag/deleteTag id sets now come from notesTouchingTag (topical ∪ label notes via new noteIds(withTaskLabel:)) — they previously MISSED notes where a tag lived only on task lines, silently skipping those files. Wand suggestions also see the union. Notes-sidebar top-10 stays topics-only by design. |
| 148 | 2026-07-16 | Editor: amplified nesting indent for subtasks (user request) | ✅ | Nested list/task lines render with real paragraph indentation (22pt per 2-space source level; wrapped text aligns past the bubble via headIndent). File bytes unchanged — display-only, like every editor treatment. Test: parent 0 < child < grandchild. |
| 149 | 2026-07-22 | App icon v2: parchment & quill (user-supplied inspiration) | ✅ | Redrawn in CoreGraphics after the user's fantasy artwork: dark slate iron frame with corner braces + double border, jagged-edged aged parchment (gradient + blotch shading + edge burn), three ink lines + indigo signature flourish, ribbon bookmark, black quill silhouette with sheen/nib/ink-dot. Stylized flat-vector homage (no text — lettering doesn't survive small sizes). All sizes regenerated. |
| 150 | 2026-07-22 | PERF (user report): snappy tab switches; ⌘F find in note | ✅ | Attempt 1 (remount all tabs + toolbarVisibility(.hidden) on inactive) REGRESSED the min-size bug — scripted AppleScript resize clamped at 1410pt (the beta doesn't unregister hidden toolbars); reverted. Real fix: SharedEditorCache in EditorKit — ONE NSTextView+coordinator per process, reused when SwiftUI rebuilds the Notes tab (guarded: cache must be detached — a second window builds fresh); unchanged text skips string-set AND restyle, so Tags→Notes no longer pays view creation + full TextKit relayout. Resize re-verified at 860×560. ⌘F: hidden shortcut → one-shot findSignal → macOS native find bar (performTextFinderAction, first-responder ensured) / iOS findInteraction navigator. |
| 151 | 2026-07-22 | FIX (user report): dead zones in top tab buttons | ✅ | Plain-style buttons hit-test only rendered pixels — the icon-text gap and capsule padding ignored clicks. .contentShape(Capsule()) after padding/background makes the entire capsule region (and around the glyphs) clickable. |
| 152 | 2026-07-22 | ⌘F match highlighting in a bold, user-chosen color | ✅ | NSTextFinder's highlight colors aren't public API — the coordinator now paints ALL matches itself: a 350ms watcher reads the system find pasteboard while the find bar is visible, restyles clean on term change, and tints case-insensitive matches with theme.findHighlightColor (vivid, α0.55). Bar closed → clean restyle, watcher exits. Settings › Editor gains a Find-highlight color picker (yellow/orange/pink/green/blue) plumbed through theme.customized; theme-change detection includes it so switching color re-applies live. macOS only (iOS system find UI owns its own highlight). |
| 153 | 2026-07-22 | Editor tags colored like To-Do chips (user request) | ✅ | .tag(name) styling now derives per-tag color identically to TaskChipStyle: same 8-color system palette order, same unicode-scalar fold, and the SAME "labelColors" UserDefaults overrides — a chip color picked in To-Do recolors the tag in every note. 2s override cache (attributes run per tag per restyle). |
| 153b | 2026-07-22 | CORRECTION: tag chips actually land this time | ✅ | Audit finding: pass 146's styler/theme tag patches NEVER applied (script aborted post-log; commits claimed the feature) — tags had no styling at all. Now verified by grep + test: StyleRanges .tag(name) kind (regex tier, code-excluded, test-pinned) + theme chip attributes with per-tag colors identical to To-Do chips (same palette/fold/"labelColors" overrides, 2s cache). STANDING RULE: patch scripts end with `&& echo PATCH_OK` and grep-verification before any gate/commit runs. |
| 154 | 2026-07-22 | Tag pills: rounded ends + vertical breathing room (user screenshot) | ✅ | Attribute backgrounds fill the whole line box with square corners — replaced by TagChipLayoutFragment: rounded-rect pills (full pill radius) drawn behind tag ranges, inset 2pt top / 2.5pt bottom from the line box and padded 4pt horizontally; per-line-fragment geometry via locationForCharacter so wrapped tags pill correctly. Theme keeps colored bold text only. Chip fragment ranks below table/code/frontmatter in the delegate (both platforms); first insert landed outside the paragraphRange scope — compile error caught it, block moved inside. |
| 155 | 2026-07-22 | Tag pills centered on glyphs (user screenshot: bottom-hugging text) | ✅ | Pills were sized from the line box while the smaller tag glyphs sat on the baseline → text bottom-aligned in the pill. Fragment now anchors the pill to the TEXT BASELINE using the tag font's ascender/descender (+2.5pt even padding), via locationForCharacter's y; tag text shrunk to 0.88× so the capsule breathes evenly on all sides; theme exposes tagChipFont for the metrics. |
| 156 | 2026-07-27 | FIX (user report): New Folder button dead | ✅ | Pass 144's New Area alert attached to the SAME view as the New Folder alert — SwiftUI honors one .alert per attachment point, so New Folder (and folder-context New Subfolder…, same state) silently never presented. Both alerts now live on separate EmptyView backgrounds. STANDING RULE noted: one presentation modifier per view. |
| 157 | 2026-07-27 | Duplicate note (user request) | ✅ | Note context menu gains Duplicate (above Move To): coordinated read → "Title copy.md" via placeholder-aware VaultNaming (counters on collision) → write beside the original → select the copy. Duplicating a LOCKED note copies ciphertext — the copy opens with the same passphrase. |
| 158 | 2026-07-27 | M9.5: Settings buildout | ✅ | One crammed pane → six: General (version, notes sort, full keyboard-shortcut reference), Editor (typography + focus toggle + find color), To-Do (density/streaks + grammar explainer), Security (app lock + ADP + locked-notes explainer), Vault (location + registered folder vaults with Remove — new management surface), AI & Import (Ollama + conversion, unchanged plumbing); Debug pane keeps the container browser in dev builds. Every persistent AppStorage knob is now discoverable somewhere. |
| 159 | 2026-07-27 | M9.6a: ?discuss/?waiting grammar + index + filters | ✅ | Research-selected design (GTD agenda/waiting split; Fellow talking-point model). Grammar: closed-set ?discuss/?waiting kind token (prose "?" never matches — test), settingKind rewrite, replacingText preserves — 3 test suites, 70 TaskEngine tests. Schema v15: task.kind indexed + peopleTasks() (open ∧ (assignee ∨ kind)). TaskFilter: @person + kind: predicates (test); TodoView filter path passes the new fields. |
| 160 | 2026-07-27 | M9.6b: People tab + 1:1 run mode — MODULE COMPLETE | ✅ | New People tab (between Tags slot ordering): sidebar lists every @person (+ Unassigned bucket for bare ?discuss) with discuss-count badges; person page = To Discuss / Delegated / Waiting sections (GTD split), rows toggle/open-detail/jump-to-note, context menu re-kinds items (task/waiting/discuss). Start 1:1 (disabled when queue empty): Fellow-style run sheet — big checkable rows, covered counter, Finish completes covered items (✅ stamps) AND appends "## 1:1 — date" + covered bullets to People/<Name>.md (vault-native meeting history; folder auto-created); uncovered items carry forward for free. TaskDetail gains a Kind segmented row. iOS gets the tab too. |
| 161 | 2026-07-27 | FIX (user reports): "missing" tasks + invisible @/? tokens — diagnosed against the live db | ✅ | Sqlite forensics: indexing was FINE (214 tasks, @Amber ?discuss present). Three perception gaps closed: (1) EMPTY "- [ ] " lines never indexed (regex required text) → scanner accepts empty; To-Do renders "(untitled task)" italic. (2) Nested subtasks were hidden by design (parent + n/m only) → open subtasks now display inline, indented under their parents (openSubtasks query; non-selectable rows; tap = details). (3) @person / ?discuss / ?waiting had ZERO editor feedback → they're chips now (indigo mentions; teal discuss / orange waiting), riding the same pill machinery as tags. |
| 162 | 2026-07-27 | People: Discussed history (user request) | ✅ | Each person page gains a collapsed "Discussed (N)" disclosure: completed ?discuss items grouped by ✅day, newest first; green check reopens (strips the token's ✅ via toggle — item returns to the agenda), jump-to-note per item, and an "Open meeting log" link to People/<Name>.md when it exists. Data via existing completedTasks() filtered person+kind — no schema change. |
| 163 | 2026-07-27 | People → Meetings (user request: committees/workgroups) | ✅ | Module reframed around AUDIENCES: any @handle (person, @curriculum-committee, workgroup) gets a meeting page — grammar unchanged (@ already accepts hyphenated handles). Renames: tab Meetings (person.3), Start Meeting, MeetingRunView, log heading "## Meeting — date", logs write Meetings/<Name>.md (legacy People/ read as fallback for the log link). Empty states teach the group convention. |
| 164 | 2026-07-27 | To-Do: meeting-membership chip (user request) | ✅ | MeetingChip in TaskChips: any task that appears on a Meetings page (assignee or kind token) wears a person.3 capsule in the To-Do list AND board/agenda/matrix cards — teal "@name"/"discuss" for agenda items, orange for waiting, indigo for delegated; hover explains membership. |
| 165 | 2026-07-27 | Task window: Cancel / Save / Save & Exit (user request) | ✅ | Semantic shift: field edits (title/due/start/priority/assignee/milestone/kind + CloudKit extras) now STAGE locally instead of committing per-change — Cancel finally means something (close discards). Save applies every diff vs the load-time baseline as ONE atomic line rewrite (sequential per-field writes invalidate each other's rawLine anchors) + extras save; Save & Exit adds dismiss; closing the window without saving = cancel. Immediate ACTIONS stay immediate by design: complete-toggle, add-dependency, add-subtask, move-to-project. Dead per-field commit helpers removed. |
| 166 | 2026-07-27 | ?next / ?someday / ?followup kinds (user-selected from design menu) | ✅ | Closed vocabulary grows to five (regexes in parser/rewriter/styler; test). To-Do: NEXT section pinned on top (green — chosen commitments, out of normal buckets), SOMEDAY parked at the bottom as a collapsed disclosure (out of every active bucket — GTD someday/maybe); followups flow through buckets normally. Meetings pages gain a Follow-ups section (blue); bare ?next/?someday WITHOUT an audience stay out of Meetings (query scoped to discuss/waiting/followup kinds). Chips: green next / gray someday / blue follow-up everywhere (editor + rows + cards). Detail-window Kind picker → menu with all six states. |
| 167 | 2026-07-27 | @ and ? autocomplete (user request) | ✅ | AutocompleteContext generalizes its back-scan to three sigils: # (non-empty body — heading guard stands), @ (audiences; empty body OK — prose "a@b" excluded by the whitespace-before rule), ? (closed kind vocabulary; "really?" excluded same way) — 2 test suites. Editor: mentionCandidates plumbed everywhere incl. the CACHED-editor branch (distinct assignees via new allAssignees query); kind pool is the static vocabulary; popup still only opens with matches and only on insertion. |
| 168 | 2026-07-27 | Editor: heading folding (user request) | ✅ | HeadingFolding (pure, 4 tests): fold range = end of heading paragraph → next heading of ≤ level (or EOF); fold identity = "level|title" keys so edits elsewhere don't unfold. Chevrons (▾/▸) drawn in the left margin via HeadingFoldFragment for every heading; margin clicks (x < inset, gesture fails outside so editing untouched) toggle; folded sections hide via the hair-height mechanism, applied last so nothing re-reveals them. iOS folds render; toggle affordance macOS-only for now. Display-only — the file never changes. |
| 169 | 2026-07-27 | M9.7: calendars in daily notes (user /loop) | ✅ | STRATEGY: EventKit covers Outlook+Google+Apple via system Calendar accounts — one native API, zero OAuth, on-device only (direct Graph/Google OAuth planned as optional later phase). CalendarService: full-access request, day-window fetch across non-excluded calendars, meetingsMarkdown ("# time — title" + three returns per event, all-day handled); calendars entitlement + usage string. Daily note creation expands the TEMPLATE ({{date}}/{{weekday}}/{{time}}/{{meetings}}), user-editable in the new Settings › Calendar pane (monospace editor, placeholder help, reset) alongside Connect flow, access states, and per-calendar include toggles. First daily note after connecting shows the day's meetings as top-level headings with note room under each. |
| 170 | 2026-07-27 | FIX (user report): black bar over Meetings detail | ✅ | The detail pane's .toolbar/.navigationTitle rendered as an opaque black strip over content under the custom macOS shell (same beta toolbar-bridge family as the .searchable/.inspector crashes). Replaced with an in-content header via safeAreaInset: person title + prominent Start Meeting button on a .bar background — content lays out below it instead of underneath it. |
| 171 | 2026-07-27 | FIX (user report ×2): Meetings bar ghosting; add-discuss field | ✅ | Detail restructured to plain VStack (opaque window-background header + Divider + List) — no safeAreaInset, no translucent material, and the split view's residual toolbar strip hidden (macOS) so nothing can slide underneath. NEW: "Add a discussion item for <person>…" field atop the To Discuss section — writes "- [ ] text @person ?discuss" into Meetings/<Name>.md (created on first use), so ad-hoc agenda items need no host note; hidden for Unassigned. |
| 172 | 2026-08-17 | Daily note: To-Do This Week / Continuous Efforts / On the Horizon (user request) | ✅ | Template gains three auto-filled sections via new placeholders: {{weektodos}} = open tasks due ≤ +7 days sorted by due, overdue marked, someday excluded; {{continuous}} = open recurring (&every) tasks with their cadence; {{horizon}} = planned-status projects (📁) + ?someday items. All render as PLAIN bullets with source-note names — checkbox lines would duplicate into the task index. dailySectionsProvider closure wires index→model (AppShell); stored legacy templates auto-upgrade (Settings' TextEditor had persisted the old default on mere open); Settings placeholder help updated. |
| 173 | 2026-08-17 | M10 deferred (user decision); shakedown phase continues | ✅ | Plan updated: M9.9 ongoing daily shakedown is the current phase — user drives via day-to-day use, reports jump the queue; release work waits for an explicit go. |
| 174 | 2026-08-20 | M9.8-01: vault context files (_index.md TOCs + CLAUDE.md seed) | ✅ | Delegated build (sonnet, worktree). Pure IndexFileRenderer in IndexKit (16 tests: ordering, truncation, locked 🔒 title-only, URL-encoding, segment omission) + extractSummary. VaultIndexService: 2s-debounced regeneration after reindex, compare-before-write (the loop breaker vs file observers), stale-index deletion, claudeIndexFiles default-on gate, one-time CLAUDE.md seed (VaultNaming.isTaken so an undownloaded placeholder can't be clobbered). _index.md excluded from every note surface via shared VaultFileStore.isIndexFile predicate applied at the two real enumeration choke points (VaultEnumerator + MetadataQueryObserver — builder traced all consumers); root CLAUDE.md stays a normal editable note. |
| 175 | 2026-08-20 | M9.8-02: MCPKit + bundled notetaker-mcp read-only stdio MCP server | ✅ | Delegated build (opus, worktree). Official MCP Swift SDK pinned 0.12.x; thin MCPServer/main + all logic in MCPKit (33 tests + in-process protocol smoke). Five read-only tools: vault_overview, list_folder, read_note (coordinated read, placeholder download wait, locked→title-only from the FULL parse on every body path), search (FTS5 BM25 + cosine over existing noteChunk embeddings, reciprocal-rank fusion k=60, punctuation-safe quoting; NLContextualEmbedding confirmed loading in a CLI process), tasks. New IndexDatabase.openReadOnly(): readonly GRDB config, 1s busy timeout, throws on user_version mismatch, never migrates/wipes. Every index-backed tool degrades to a .md scan with "source":"scan". project.yml: type-tool target, macOS-only via destinationFilters, copy-files into Contents/MacOS, hardened runtime + real signing. Verified standalone against the real vault with the app closed (1136 notes, hybrid search hits, scan fallback demonstrated). Path-escape rejection; --vault/--index overrides. |
| 176 | 2026-08-20 | M9.8-03: Claude integration Settings + docs | ✅ | Delegated build (sonnet, worktree). Settings › AI & Import gains "Claude integration": claudeIndexFiles toggle (default on, off-state caption), macOS-only MCP block (resolved bundle binary path with missing-binary warning state, copyable `claude mcp add notetaker -- "<path>"` via NSPasteboard, Claude Desktop note, read-only/works-closed/locked-notes caption). App never writes ~/.claude.json or Claude Desktop config (secrets policy). Docs: FEATURES/README/CLAUDE.md/PLAN M9.8 milestone. Merge integration: three worktree commits cherry-picked onto main (one CLAUDE.md conflict vs the doc-refresh commit, hand-resolved), full verify gate green on the merged tree (391 package tests incl. 49 new, both builds, launch OK), notetaker-mcp present/signed/running in the installed app. |
| 177 | 2026-08-20 | Editor: table editing UX (user /delegate — MarkupEditor research spawned it) | ✅ | Research verdict: MarkupEditor is WKWebView+ProseMirror HTML with zero markdown model — adopting it would mean a lossy JS-side HTML↔md bridge re-implementing the task grammar; user chose native TextKit 2 improvements instead (specs/editor/). This pass (opus, worktree): pure TableEditing over the existing TableGrid parse — cellAt/next/prev with pipe-adjacent binding, Tab-in-last-cell row growth, minimal-edit row ops, splice-preserving column ops, idempotent :-preserving alignment; doCommandBy Tab/Backtab/Return interception gated on a cheap pipe-on-line check (list continuation proven intact by live-NSTextView key tests); align-on-leave via anchor+snapshot, Task hop out of the delegate callback, single undo group; five no-op-safe table EditorCommands + insert-menu Table section. 33 new tests (27 pure + 6 real-coordinator key tests). |
| 178 | 2026-08-20 | Editor: rich paste — HTML/RTF→markdown, URL link-wrap, image paste/drop | ✅ | (sonnet, worktree) Pure RichPaste converter: paragraph classification + merged inline runs → bold/italic/strike/code/links/headings (dominant-font-ratio tiers)/lists/quotes, content never dropped; isProbablyMarkdown keeps markdown pastes verbatim; linkWrapping turns URL-over-selection into [text](url). New MarkdownTextView/MarkdownUITextView subclasses own the 5-step paste ladder (Paste-and-Match-Style stays raw; any failure → plain paste); macOS subclass rebuilt on the same TextKit 2 container so SharedEditorCache semantics survive. Image paste/drop → importAttachments closure → NotesModel.attachImage (new data variant, temp-file + coordinated copy into Attachments/) → ![name](path) inserted; index-aligned results so partial failures can't mis-pair. 21 new tests. |
| 179 | 2026-08-20 | Editor: selection-aware bar, link editing popover, image sizing + click-to-open | ✅ | (sonnet, worktree) Pure SelectionContext.at over the ranges the last restyle already produced (Equatable-diffed publishes, no reparse per caret move) → onSelectionContext; format bar lights active bold/italic/strike/code/list/quote, heading menu shows current level, wrap buttons dim in code blocks. ⇧⌘K in an existing link opens a pre-filled popover (anchored to the bar button, not .toolbar — beta trap avoided): Save rewrites via .editLink, Remove unwraps. ImageLayoutFragment: 280pt cap (theme spacing reservation raised to match), aspect preserved, rounded corners, hitTestsImage + NSEvent monitor (fold-click pattern) → onOpenAttachment → NSWorkspace, resolving vault-relative paths against the same base the thumbnail used. 17 new tests. MERGE: three worktree commits cherry-picked; conflicts in the selection-change handler and editor init hand-resolved (order: table tracking → context publish → mode-gated restyle); gate green — EditorKit 140 tests (71 new), both builds, launch OK. Builders honestly reported no visual UI verification possible (locked/headless session) — interactive behaviors covered by unit/key tests; user's daily shakedown is the live check. |
| 180 | 2026-08-20 | FIX (critic round 1, verdict revise): editor hardening | ✅ | Three blocking issues fixed. (1) updateTableTracking crashed on stale tableRegions (regions refresh in restyle AFTER the selection notification; delete-through-table left a region past the text) — bounds guard added; regression key test PROVEN against the unfixed code (uncaught NSException) before the fix. (2) editLink/removeLink rewrote a popover-open-captured range with only a length check — commands now carry the link's exact expected source and no-op when the document drifted (sync under an open editor); drift test added. (3) Link editor popover sat anchored inside the safeAreaInset format bar (beta toolbar-bridge family, unverifiable headless) — converted to a sheet (proven pattern: palette/lock), Cancel/esc added. Plus critic's top non-blocking: raw-bitmap paste only imports as image when no text flavor accompanies it, so Word/Excel pastes convert instead of importing their TIFF rendition; screenshots still import. EditorKit 142 tests; full gate green. Remaining non-blockings recorded in specs/editor/00-overview.md. |
| 181 | 2026-08-20 | FIX (user reports ×2): blank notes; translucent bar over the editor | ✅ | (1) Blank notes — pass 178's factory-then-swap NSTextView construction reattached the stock view's text container to the MarkdownTextView subclass, but the TextKit 2 layout manager kept rendering into the DETACHED original: visible view empty. Fixed with MarkdownTextView.makeTextKit2() building a fresh stack (contentStorage → layoutManager → container, storage strongly retained — the public accessors reach it through weak links); regression test asserts storage sync AND layout-fragment production on THIS view's container. (2) Bar — formatBar sat in .safeAreaInset; editor content scrolled visibly through it (Meetings-header beta family). Moved in-content: VStack(bar on opaque Color.headerBackground + Divider + editor), safeAreaInset removed. (3) Bonus, caught BY the gate mid-fix: intermittent SIGSEGV in EditorKit tests — RichPasteTests was the one AppKit-touching suite not @MainActor; NSFont attribute casts on Swift Testing's cooperative threads segfaulted ~1 in 6 runs (crash report: swift_dynamicCast in inlineMarkdown). Marked @MainActor per the file's own convention; 14/14 consecutive full-suite runs clean. Gate green. |
| 182 | 2026-08-20 | FIX (user report): translucent strip still covering the editor's format bar | ✅ | Pass 181 made the format bar opaque but the strip ON TOP of it survived: the NOTES DETAIL PANE still declared .toolbar + .navigationTitle, which the beta bridge renders as a translucent band over detail content (Meetings bug, third sighting). Applied MeetingsView's full cure. Detail toolbar items (AI menu, Lock, Make Project, Graph, Focus, Info, daily prev/next) moved in-content into `noteActionBar`, a fixed cluster beside the scrolling format bar inside a single opaque `editorHeader` row. Hiding the window toolbar is window-wide, so the SIDEBAR's toolbar items (New Folder, templates, Today, vault switcher + its fileImporter) folded into a new "⋯" `sidebarMoreMenu` in the existing action strip; sidebar .toolbar deleted. Then .toolbar(.hidden, for: .windowToolbar) on macOS; iOS keeps navigationTitle + inline display mode. Shortcuts preserved: ⌘N on the visible New note button, ⌥⌘0 on Info, ⇧⌘D on a hidden button (menu-item key equivalents only register while the menu is mounted). All SF Symbol names verified to exist before use (chart.gantt lesson). Gate green. |
| 183 | 2026-08-20 | FIX (user crash report): opening the link graph killed the app | ✅ | Crash log: EXC_BREAKPOINT in _NativeDictionary.merge(trappingOnDuplicates:) ← Dictionary(uniqueKeysWithValues:) ← GraphView.layout, presenting the Graph sheet. GraphView keyed notes BY TITLE — two notes in different folders share a title, so on a real vault (1136 notes) the graph trapped every time. Pre-existing bug; the relocated toolbar button just made it reachable again. Swept the codebase for the same trapping initializer and found two more of the same class: ProjectSchedule keyed nodes by ^id (hand-editable markdown — copy a task line and Projects crashes identically) and NotesModel's id lookup (safe today, hardened anyway). All now Dictionary(_:uniquingKeysWith:) with first-wins so ordering stays stable across renders. ProjectKit regression tests added and PROVEN against the unfixed scheduler ("Fatal error: Duplicate values for key: 't-1'") before confirming green. Gate green. NOTE: GraphView/NotesModel live in the App target, which has no test target — their fix rests on the ProjectKit tests plus the shared pattern. |
| 184 | 2026-09-15 | Editor: Live Preview core — incremental restyle + Typora-style narrow reveal (user /delegate, rendering plan spec 02) | ✅ | Research verdict first: the "clumsy" live preview is the documented failure mode of hide/reveal itself (Obsidian forums), no Swift package solves it, web editors named by the user (editor.md stale, Toast UI archived) contribute UX ideas only. This pass (opus, worktree): RevealScope {selection, blockRange} replaces the paragraph-wide hide range — block markers reveal for the caret's line, inline markers only for the span(s) containing/touching the caret or intersecting the selection. MarkdownHighlighter.updateReveal re-applies attributes only in paragraph windows where a marker/block verdict flipped, through the SAME window-clipped applyAttributes that the full highlight uses, so full-at-B == full-at-A + update(A→B) by construction (per-character property test over 9 fixtures, mutation-tested). Coordinator caches the parse (EditorParseCache) and never calls MarkdownStyler.styleRanges on a caret-only move (asserted via a DEBUG parse counter); a text change still parses exactly once. SyntaxMarkers.markerGroups (MarkdownKit) groups markers by owning span; markerRanges output unchanged. +21 EditorKit, +5 MarkdownKit tests. |
| 185 | 2026-09-15 | Editor: tables keep their grid while editing; frontmatter as a stable properties card (spec 03) | ✅ | (opus, worktree) TableStyling: monospaced tableFont over the whole table, pipes dimmed at full size in BOTH caret states, separator row dimmed not hidden; TableRowLayoutFragment draws header tint + rules over the visible text in both states (no more cell re-drawing; rule width from the row's typographic bounds so rules meet the pipe glyphs). FrontmatterStyling + FrontmatterLayoutFragment: identical font metrics inside and outside, only colors change; card background spans the container width. Both passes run AFTER the marker loop so no 0.01pt run can misalign a column. Live-NSTextView layout-height tests: table and frontmatter heights equal within 0.01pt caret-out vs caret-in; offscreen cacheDisplay renders were byte-identical PNGs. +19 EditorKit tests. MERGE: highlighter conflict with 184 resolved (02's reveal scaffolding, 03's stable-block calls keyed off reveal.revealsBlock; tables dropped from revealDelta since they are caret-independent now); 03's tests migrated to the reveal: API. Merged suite 183/183 ×5. |
| 186 | 2026-09-15 | Editor: Reading mode + three-mode model — Source / Live / Reading (spec 01) | ✅ | (opus, worktree) New Packages/ReadingKit: ReadingDocument builds [ReadingBlock] from the swift-markdown AST with 0-based FILE line numbers (same coordinate as TaskRecord.line); extended tokens found by consuming MarkdownStyler.styleRanges (parity asserted set-for-set), task lines through NoteScanner → TaskTokenParser (byte-identical to the index); ReadingView renders headings/nested lists/tappable task circles/token chips (colors from TaskChipStyle, new kindColor extracted from MeetingChip)/wikilinks/links/images (280pt cap)/Grid tables/code cards/quote bars/frontmatter card/rules; build runs off-main. App: EditorMode persisted in @AppStorage, ⌘E cycles Source→Live→Reading, ⌘/ toggles Source↔Live, three-segment picker in editorHeader between the format bar and the action cluster (formatting controls hidden in Reading), Settings › Editor default-mode picker + "Source mode uses a monospaced font". Position sync: caret line → block anchor (editor→Reading), top visible block → scrollTarget (Reading→editor). Reading task toggles go through indexService.toggle (noteText never mutated). ⌘E collision: inline code moved to ⌥⌘C. 33 ReadingKit tests. Gate on the merged tree: 545 package tests, both builds, launch OK. NOT observed live by anyone (builders declined to launch against the live vault) — ⌘E cycling, the picker, Reading checkbox toggles, and the no-reflow feel are the user's shakedown checks. |
| 187 | 2026-09-15 | FIX (critic round 1, verdict revise): folded tables re-inflating; Focus mode froze the narrow reveal | ✅ | (1) TableStyling.apply / FrontmatterStyling.apply wrote to the WHOLE storage, bypassing updateReveal's window clip, and ran before the window-clipped fold pass — so any caret move re-asserted full-size font on a table inside a folded section: ghost grid, everything below shifts (the exact reflow class this work removes). Both now take `clip:` and route every write (incl. dimPipes) through a bounds-clipped helper; applyAttributes passes its window. Two fixtures added to revealFixtures (fold+table, inline marker inside a cell) — PROVEN to fail on the unfixed code. (2) Focus mode early-returned on same-paragraph caret moves, so span delimiters never appeared; now falls through to applyRevealUpdate with dimOutside: cursor (the dim region is unchanged — updateReveal's documented precondition). Regression test PROVEN to fail without the fix. Plus the critic's top non-blocking: Reading-mode checkbox now verifies the current text at that line matches the index row's rawLine before toggling — a stale index (autosave + reindex lag) no-ops instead of flipping a neighbour. EditorKit 184 tests; full gate green. |
| 188 | 2026-09-15 | FIX (critic round 2, verdict revise): Source+Focus leaked Live Preview styling | ✅ | Regression introduced by pass 187's Focus fix: the same-paragraph Focus path reached applyRevealUpdate regardless of livePreview, which always built a non-nil RevealScope — so in Source mode with Focus on, a caret move hid markers elsewhere on the line and applied the frontmatter card. Both coordinators now build the scope the way restyle does (`livePreview ? RevealScope.at(…) : nil`), so Source+Focus is a no-op on caret moves. Regression test sourceModeWithFocusStaysFullyRevealed PROVEN to fail on the unfixed code (three expectations) and pass with the fix. Critic also confirmed in round 2: window clipping is a no-op on the full highlight path (StableBlockTests height guarantees intact), dimOutside: cursor in the Focus path is equivalent to a full restyle's dim, and the Reading-toggle rawLine guard has no hole (CRLF, frontmatter offsets, locked notes). EditorKit 185 tests; full gate green. |
| 189 | 2026-09-15 | Editor: per-mode font family and size + ⌘=/⌘−/⌘0 zoom (user /delegate, spec 04) | ✅ | (sonnet, in place) Pure EditorFontPreferences in EditorKit: per-mode keys (Live keeps the legacy editorFontDesign/editorFontSize keys; Source/Reading get their own), 11…28 clamp, one-time idempotent migration seeded from the old sourceModeMonospace toggle and the shared size (store abstracted behind EditorFontStore so tests use a dictionary; UserDefaults conforms). EditorMode moved into EditorKit with raw values intact. Settings › Editor: "Fonts" section with Source / Live Preview / Reading rows (design picker + size stepper + per-row Reset; Source resets to Monospaced) replacing the single picker/stepper/toggle. NotesView: each mode's theme/readingStyle from its own keys; hidden ⌘= / ⌘− / ⌘0 buttons resize the ACTIVE mode's stored size (grep-verified no collisions; ⌥⌘0 inspector intact). Migration called from NotetakerApp.init. Builder found deferral #5 (font design not repainting) was STALE — modeChanged has compared fontDesign since July — and left MarkdownEditor untouched; overview corrected. 12 new tests (EditorKit 197); full gate green. Not observed live (builder did not drive the UI) — Settings rows, zoom keys, and immediate repaint are shakedown checks. |
| 190 | 2026-09-15 | Editor: heading size setting (user: "really just the headers to be smaller") | ✅ | One global "Heading size" percent (Settings › Editor › Fonts, 0…120%, step 10, reset 100) stored as @AppStorage("headingScale") and applied in every mode: MarkdownTheme.headingScale and ReadingStyle.headingScale scale each level's default ENLARGEMENT (size = base × (1 + (levelScale − 1) × headingScale)), so 100% is unchanged, 50% halves the extra, 0% makes headings body-sized — levels stay ordered and never drop below body text. Threaded through MarkdownTheme.customized(headingScale:) and ReadingStyle(headingScale:) from NotesView. I first read the request as code blocks; the user corrected mid-turn to headings — no code-block scaling was added. Tests in EditorKit (ThemeTokenTests) and ReadingKit for shrink/floor/ordering. Full gate green. Not observed live — the Settings stepper is a shakedown check. |
| 191 | 2026-09-17 | Proofing: system spelling + grammar, token-aware; autocorrect off (user /delegate research → options → spec 01) | ✅ | Research (sonnet): Apple's checkers are the on-device foundation (grammar macOS-only), LanguageTool the strong optional layer; Harper has no Swift binding; user picked system + LanguageTool, autocorrect OFF. Empirical: a fresh NSTextView here had spell/grammar OFF and autocorrect ON. Builder (opus, worktree) STEP 0 finding: TextKit 2 keeps the squiggle as a `.spellingState` RENDERING attribute on NSTextLayoutManager — survives restyle/updateReveal/edits, no mitigation needed (tests lock it). BLOCKER found: `isContinuousSpellCheckingEnabled` silently bounces to false unless `NSAllowContinuousSpellChecking` reads true, and this machine's NSGlobalDomain holds 0 — the real reason the editor never squiggled. Pure `ProofingExclusions.ranges` (code, frontmatter, link destinations only — labels stay checkable — images, wikilinks, chips, task tokens via NEW `TaskTokenParser.tokenRanges` so there is one parser); `didCheckTextIn` filters annotations (always keeps .orthography); `willCheckTextIn` clears the grammar bit when off; `writingToolsIgnoredRangesInEnclosingRange` implemented substring-relative per UIKit's documented contract (AppKit's header has none). Settings › Editor "Proofing": spelling/grammar(macOS)/autocorrect toggles, autocorrect default off. MERGE change: the default write moved OUT of EditorKit into `ProofingBootstrap.allowContinuousSpellChecking()` called from NotetakerApp.init before any view — removes the builder's degraded-first-launch caveat and keeps the package free of app-domain writes. Known limit: the right-click suggestion menu still offers guesses for a word inside a token (user-initiated, nothing auto-rewrites). +26 EditorKit, +7 TaskEngine tests. |
| 192 | 2026-09-17 | Proofing: self-hosted LanguageTool provider + Proofread panel (spec 02) | ✅ | (sonnet, worktree) New Packages/ProofKit, dependency-free: GrammarProvider/GrammarMatch/GrammarError, LanguageToolProvider over POST /v2/check with annotated-text `data` so excluded spans go as markup; `AnnotatedText.build` tiles the original exactly and LT (Java) offsets are already UTF-16 units — no conversion, locked by an emoji round-trip test; withTimeout 8 s; typed errors. App: Proofread sheet (⇧⌘P + note-action button `text.badge.checkmark`), whole note or selection, Apply via NEW `MarkdownEditing.replaceRange(range:expected:with:)` with the .editLink drift guard, later matches shifted by offset arithmetic (`GrammarMatch.shifted`, drops stale overlaps), Ignore, running/none/error states; Settings › AI & Import "LanguageTool" section with Keychain-backed URL (account languageToolURL) + Test connection via /v2/languages. MERGE: the builder's temporary exclusion shim swapped for `ProofingExclusions.ranges` — one oracle for both checkers. No LanguageTool URL is configured on this machine (checked via the app's own KeychainStore API), so no live proofread was run. 27 ProofKit + 3 EditorKit tests. Gate on the merged tree green. Homelab one-liner: `docker run -d --name languagetool -p 8010:8010 erikvl87/languagetool`. |
| 193 | 2026-09-17 | FIX (critic round 1, verdict revise): LanguageTool could delete a token; Proofread live in Reading mode | ✅ | (1) LanguageTool drops markup from the text it analyzes, so `fix teh bug #urgent today` is checked as `fix teh bug  today`; its whitespace rule returns a range that, in the ORIGINAL text, is ` #urgent ` — one Apply would have deleted the tag on essentially every task line. `LanguageToolProvider.check` now drops any match touching an excluded span (same last line of defense the system checker has in `didCheckTextIn`); test stubs exactly that straddling match and keeps only the real typo. (2) The Proofread button/⇧⌘P were enabled in Reading mode, where no MarkdownEditor consumes the Apply command — clicks looked applied, and the last stale command fired later on mode switch. Gated to editing modes; entering Reading clears any pending editorCommand. Also taken from the non-blocking list: both link markers excluded (LT saw an orphan `[` per link → UNPAIRED_BRACKETS noise); selection scope now requires the first responder to be the MarkdownTextView (the sidebar search field's editor qualified before); Keychain probe for the LanguageTool URL memoized 2 s instead of per keystroke. Gate green. |
